# Disable lifecycle scripts to prevent potential security issues
ignore-scripts=true
# Save exact versions to ensure consistent dependencies across environments
save-exact=true
# Set a long minimum release age to avoid installing recently published packages
minimum-release-age=10000
# Block installation of exotic sub-dependencies
block-exotic-subdeps=true
# Prevent downgrading of trusted packages
trust-policies=no-downgrade