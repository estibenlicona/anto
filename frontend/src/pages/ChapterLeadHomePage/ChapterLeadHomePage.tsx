import React from "react";
import { ControlTowerContainer } from "@features/control-tower/ControlTowerContainer";

// El h1 visible lo pone la propia Torre de control.
export const ChapterLeadHomePage: React.FC = () => {
  return <ControlTowerContainer />;
};
