---
"@tuya-ui/components": patch
---

`Tag` baja el texto de bold a medium — el mismo peso que `Badge`. El texto de un tag identifica una categoría, no la enfatiza: en bold, una fila con varios tags (los stacks del listado de personas) hacía más ruido que el dato principal de la fila.
