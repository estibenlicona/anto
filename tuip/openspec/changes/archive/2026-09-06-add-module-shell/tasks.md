## 1. Colapso compartido y ModuleShell

- [x] 1.1 Extraer de `packages/components/src/app-shell.tsx` la lectura/escritura de `tuya-ui:sidebar-collapsed` y el auto-colapso por `matchMedia("(max-width: 1119px)")` a `src/lib/use-collapsed-navigation.ts` (`useCollapsedNavigation(defaultCollapsed?) → { collapsed, toggle }`), y hacer que `AppShell` lo consuma. Verificar: `pnpm --filter @tuya-ui/components test` pasa sin tocar `app-shell.test.tsx`.
- [x] 1.2 Crear `src/module-shell.tsx` (`ModuleShell`, `displayName`, JSDoc en cada prop): props `groups`, `activeId`, `onNavigate`, `defaultCollapsed?`, `density?`, `sidebarFooter?`, `ariaLabel?` (nombra al módulo), `topOffset?` (px, default `0`), `className?`, `children` — sin `product`: la columna no tiene cabecera de marca porque el título es de la barra del host. `<aside>` sticky bajo la barra (variable CSS `--tuya-ui-shell-top` fijada desde `topOffset`) que contiene sólo `Sidebar` en modo controlado (`collapsed`/`onCollapsedChange`, `collapsible` por defecto) para que su franja de colapso al pie sea el control, y columna de contenido con los hijos, sin `<header>` ni hamburguesa. Verificar: `pnpm --filter @tuya-ui/components lint` (tsc) limpio.
- [x] 1.3 Control de colapso al pie: la franja de `Sidebar` (borde a borde, chevron, rótulo "Colapsar"/"Expandir") es el último elemento de la columna y el único control; colapsada conserva la franja (rótulo "Expandir" sr-only). Verificar con `src/module-shell.test.tsx`: el primer hijo de la columna es el landmark de navegación (sin cabecera ni botón encima), no se renderiza ningún `header` ni hamburguesa, la franja alterna el ancho y cambia su rótulo, sigue presente colapsada.
- [x] 1.4 Tests de persistencia y `topOffset`: colapsar desde la franja escribe `tuya-ui:sidebar-collapsed`; con la clave en `"true"` nace colapsado; `defaultCollapsed` manda; con `topOffset={56}` la raíz fija `--tuya-ui-shell-top: 56px` y el `aside` lleva `top-[var(--tuya-ui-shell-top)]` y `h-[calc(100vh-var(--tuya-ui-shell-top))]`; sin él, `0px`. Verificar: `vitest run src/module-shell.test.tsx` en verde.
- [x] 1.5 Exportar `ModuleShell` y `ModuleShellProps` desde `src/index.ts` y declararlo en `registry/definitions.ts` (`category: "layout"`, `status: "beta"`, `dependencies: ["utils", "icon", "sidebar"]`). Verificar: `pnpm --filter @tuya-ui/components build` genera `registry.json` con la entrada `module-shell`.

## 2. Campana opcional en Navbar

- [x] 2.1 Agregar `showNotifications?: boolean` (default `true`, JSDoc: "con `false`, `notifications` se ignora") a `NavbarUtilitiesProps`, `NavbarProps` y `AppShellProps` en `src/navbar.tsx` / `src/app-shell.tsx`; con `false`, `NavbarUtilities` no renderiza el `NotificationMenu` ni su botón. Verificar: `tsc --noEmit` limpio.
- [x] 2.2 Tests en `src/navbar.test.tsx` (y uno en `app-shell.test.tsx`): con `showNotifications={false}` no existe el botón "Notificaciones" y sí siguen la marca, "Ayuda" y la cuenta; sin la prop, el botón sigue presente. Verificar: `vitest run` en verde.

## 3. Documentación y catálogo

- [x] 3.1 `apps/docs/src/content/module-shell.tsx` (uso, anatomía, accesibilidad, criterio de elección) registrado en `apps/docs/src/content/index.ts`, y ejemplos `apps/docs/src/examples/module-shell/01-modulo-bajo-navbar.tsx` (Navbar del host arriba + ModuleShell con `topOffset={56}`) y `02-colapsado-inicial.tsx`. Verificar: `pnpm run docs:dev` muestra la página con ambos ejemplos operando la hamburguesa.
- [x] 3.2 Ejemplo `apps/docs/src/examples/navbar/NN-sin-notificaciones.tsx` y nota en `content/navbar.tsx`; actualizar la guía de elección en `content/app-shell.tsx`, `content/navbar.tsx` y `content/sidebar.tsx` con el criterio de cuatro casos (AppShell / ModuleShell / Navbar / Sidebar) y el reparto host↔módulo. Verificar: los tres textos remiten a `ModuleShell` y el sitio docs compila.
- [x] 3.3 Verificar que ninguna clase nueva quede fuera de la hoja publicada: `pnpm --filter @tuya-ui/components build` y `grep` de las utilidades usadas en `module-shell.tsx` sobre `dist/styles.css` (ver `scripts/build-css.ts` y `SCREEN_ONLY_TOKENS`); `verify:colors` y `verify:stylesheet` en verde.

## 4. Publicación

- [x] 4.1 `pnpm changeset` → `.changeset/add-module-shell.md` con `"@tuya-ui/components": minor` describiendo `ModuleShell`, `showNotifications` y el refactor interno de `AppShell`. Verificar: el archivo existe y `changeset status` lo lista.
- [x] 4.2 (publicado como `0.2.3`: `publish:local` siempre sube PATCH sobre el MINOR fijado a mano; tokens `0.1.17`) Fijar `"version": "0.2.0"` en `packages/components/package.json` y correr `pnpm run publish:local`. Verificar: `.local-packages/tuya-ui-components-0.2.0.tgz` existe (y el nuevo PATCH de tokens), y `tar -tzf` muestra `dist/module-shell*` y `dist/styles.css`.
- [x] 4.3 (pin de frontend intacto en 0.1.14 — su `M` en git es previo a este change; el sync del delta lo ejecuta `/opsx:archive`) Actualizar `tuip/openspec/specs/component-library/spec.md` al archivar (sync del delta) y confirmar que `frontend/package.json` sigue apuntando a `0.1.14` sin cambios. Verificar: `git status` en `frontend/` no muestra `package.json` modificado.
