## 1. Componente

- [x] 1.1 Crear `packages/components/src/notification-menu.tsx`: `NotificationMenu` (root, envuelve `DropdownMenu.Root`/`Trigger asChild`/`Portal`/`Content`), `NotificationMenuHeader`, `NotificationMenuItem`, `NotificationMenuFooter`. **Desviación del plan:** ni el proposal ni design.md contemplaban una quinta pieza, y la decisión "Alto máximo con scroll" de design.md no precisó qué componente debía dar ese `max-h/overflow-y-auto` — al implementarlo sobre `NotificationMenu` directamente (sin distinguir header/lista/footer dentro de `children`) el scroll se habría llevado puestos el header y el footer, que la fuente muestra fijos. Se agregó `NotificationMenuList`, análoga a `CardBody`/`ModalBody`/`DrawerBody`, como la única pieza que scrollea.
- [x] 1.2 `NotificationMenu` props: `trigger` (nodo), `children`, `align?`, `side?`, `className?` — mismo shape que `Menu`. Estilo del panel: `w-[380px] rounded-control border border-neutral-default bg-neutral-default shadow-md z-menu overflow-hidden`, más `flex flex-col` para que header/lista/footer se apilen y solo la lista (vía `NotificationMenuList`) reciba el scroll.
- [x] 1.3 `NotificationMenuHeader` props: `title` (nodo), `action?` (nodo, p. ej. "Marcar todas leídas", renderizado como `DropdownMenu.Item` con `text-brand-default`) — borde inferior `border-neutral-default`.
- [x] 1.4 `NotificationMenuItem` props: `unread?` (boolean, default false), `label`, `detail?`, `timestamp`, `variant?` ("success" | "info" | "warning" | "danger" | "discovery" | "neutral", default "neutral"), `onSelect?` — cada prop con JSDoc.
- [x] 1.5 `NotificationMenuItem` resuelve `unread` en dos señales a la vez: `true` → `bg-neutral-default font-semibold`; `false` → `bg-neutral-subtlest` (peso regular) — un solo condicional, no dos props independientes. Punto de color: mismo mapeo que `ActivityTimelineItem` (`bg-{role}-bold`, `neutral` con `bg-current text-neutral-subtle`), declarado localmente. Divisor inferior: `border-neutral-default`. **Ajuste sobre el plan:** el estado de resaltado (hover) se resolvió con `data-[highlighted]:bg-neutral-subtlest` en vez de la pseudo-clase `hover:` — cada fila es un `DropdownMenu.Item` navegable por teclado, y `hover:` no se activa al recorrer con flechas; `data-[highlighted]` sí, porque es el atributo que Radix aplica en ambos casos. Mismo criterio que ya usa `MenuItem`.
- [x] 1.6 Lista de notificaciones dentro de `NotificationMenuList`, con `max-h-[500px] overflow-y-auto` (aproxima "máx. 5 visibles" sin cortar los `children`).
- [x] 1.7 `NotificationMenuFooter` props: `children` (nodo, p. ej. "Ver todas") — renderizado como `DropdownMenu.Item` con `text-brand-default`, `text-center`, fondo `bg-neutral-subtlest`.
- [x] 1.8 `NotificationMenu.displayName`, `NotificationMenuHeader.displayName`, `NotificationMenuList.displayName`, `NotificationMenuItem.displayName`, `NotificationMenuFooter.displayName` asignados.

## 2. Registro

- [x] 2.1 Agregar entrada `notification-menu` al registro: categoría `overlays`, `status: "stable"`, `npmDependencies: ["react", "@radix-ui/react-dropdown-menu"]`, `dependencies: ["utils"]`.
- [x] 2.2 Agregar `export * from "./notification-menu"` a `packages/components/src/index.ts`.
- [x] 2.3 Ejecutar `pnpm --filter @tuya-ui/components build` para regenerar `registry.json` y confirmar que el componente extrae props, peso y código fuente correctamente. 34 componentes, `notification-menu: 5 exported component(s), 17 own prop(s)`.

## 3. Documentación

- [x] 3.1 Crear `apps/docs/src/content/notification-menu.tsx` (uso, anatomía, accesibilidad), incluyendo la guía "solo eventos accionables" tal como la trae la fuente. La figura de anatomía usa una réplica estática (mismas clases que el componente real, sin `DropdownMenu` interactivo) porque un panel cerrado no monta su contenido — mismo trade-off ya aceptado para Modal y Drawer.
- [x] 3.2 Crear ejemplos en vivo en `apps/docs/src/examples/notification-menu/*.tsx`: las cuatro notificaciones reales de la fuente (crítica sin leer, aprobación sin leer, dos informativas leídas), con "Marcar todas leídas" y "Ver todas", ancladas a un botón con el ícono `notification`. Se agregó un segundo ejemplo con 9 notificaciones para ejercitar el scroll de `NotificationMenuList` (cubre la tarea 4.2).
- [x] 3.3 Registrar `notificationMenuContent` en `apps/docs/src/content/index.ts` con la clave `"notification-menu"`.

## 4. Cierre

- [x] 4.1 Levantar el sitio de docs y verificar: navegación por flechas entre notificaciones y las acciones de header/footer, Escape cierra devolviendo el foco al disparador, distinción visual leída/no-leída (fondo y peso juntos), colores de punto por rol. Verificado por CDP: los colores de punto resuelven exacto (`rgb(142,15,24)`=`#8E0F18` danger, `rgb(181,122,0)`=`#B57A00` warning, `rgb(85,85,94)`=`#55555E` neutral, los tres tokens leídos directamente del DOM renderizado). **Bug encontrado y corregido acá:** con las 4 notificaciones del primer ejemplo, el contenido medía 404px contra el `max-h-[400px]` original — 4px de sobra alcanzaban para mostrar una barra de scroll innecesaria. La estimación de "cinco filas ≈ 400px" estaba mal calculada (cada fila mide ~101px, cinco son ~505px); se subió a `max-h-[500px]` en el componente, la réplica de anatomía y design.md. Re-verificado: con 4 ítems, `listScrollH === listClientH` (sin scroll); con 9, escala a 500px y sí scrollea.
- [x] 4.2 Verificar con más de cinco notificaciones que la lista desplaza en vez de cortar: todas siguen presentes en el DOM y alcanzables por teclado, el contenedor solo recorta visualmente. Verificado con el ejemplo de 9 notificaciones: `listScrollH` (1107px) > `listClientH` (500px), y 10 pulsaciones de flecha abajo (9 notificaciones + footer) terminaron el foco en "Ver todas" — Radix desplazó la lista sola para mantener visible cada ítem enfocado según se recorría.
- [x] 4.3 Grep de valores hex/px sueltos en `notification-menu.tsx`. Aparecieron más de los dos anticipados: además de `w-[380px]` y `max-h-[500px]`, también `px-[18px]` (padding horizontal de fila, sin paso entre `px-4` y `px-5`), `h-[7px]`/`w-[7px]` (diámetro del punto, sin paso entre `w-1.5` y `w-2`) y `grid-cols-[8px_1fr]` (arbitrario por necesidad — Tailwind no tiene utilidad para una columna fija más una flexible). Los seis están documentados en design.md; `py-3.5` (14px) no es arbitrario, ya es un paso real de la escala.
- [x] 4.4 Ejecutar `pnpm lint`, `pnpm test` y `pnpm build` en la raíz del monorepo y dejar los tres en verde.
