## 1. Tokens — estilo de cifra

- [x] 1.1 `packages/tokens/src/typography.ts`: agregar el peso `bold` (700) a `fontWeight` y actualizar su comentario ("Three weights in use: body, interface emphasis, and titles"), que quedaría desactualizado, dejando documentado el rol del peso nuevo — es lo que evita que se use como un peso suelto sin dueño (ver design.md).
- [x] 1.2 `packages/tokens/src/typography.ts`: agregar el estilo de cifra a `textStyle`, con su propio tamaño, alto de línea y el peso `bold` de la tarea 1.1, documentando su rol en el comentario del token igual que se hace con `display`. Se agregó `metric` (40px / 40px / 700 / tracking -0.04em), al final de la escala por ser un eje distinto al de títulos-a-etiqueta.
- [x] 1.3 Actualizar el comentario de cabecera de `textStyle` ("seven styles, and no eighth... the fix is to change level, not to invent a size"): la escala sigue cerrada, pero el conteo y la redacción deben reflejar el rol nuevo y por qué se agregó, para que no se lea como una contradicción. Nota: con `metric` la escala pasa a tener siete estilos de verdad — el comentario decía "seven" cuando eran seis.
- [x] 1.4 Verificar que el estilo nuevo sale por `fontSizeScale()` en `packages/tokens/src/tailwind-preset.ts` (deriva de `textStyle`, así que no debería requerir cambios) y que la utilidad correspondiente se genera, incluido el peso. Confirmado: sin cambios en el preset; `tokens.css` genera `--text-metric-*` con los cuatro valores.
- [x] 1.5 Agregar la escala tipográfica al `safelist` de `packages/components/tailwind.config.js`. Descubierto al consumir el token desde la app: el CSS publicado se genera escaneando solo `packages/components/src`, y `text-display`, `text-heading-lg`, `text-heading-md` y `text-metric` no las usa **ningún** componente de tuip, así que las cuatro se purgaban antes de llegar al paquete. El `safelist` ya existía con un comentario que describe exactamente este caso (`bg-neutral-canvas`). Efecto colateral: se reparó el título de la pantalla de Personas, que llevaba renderizando a 16px/400 en vez de 24px/600 porque `text-heading-lg` nunca llegaba al CSS.

## 2. Tokens — degradado de marca

- [x] 2.1 Definir el token de degradado de marca, con sus extremos tomados de pasos de la escala primitiva `brand` (no hexadecimales sueltos), y exportarlo desde el índice de tokens. Nuevo `packages/tokens/src/gradient.ts` (`brand`: `brand.500` → `brand.400`), exportado desde `tokens.ts` y sumado al objeto `tokens`; se agregó también su CSS var en `scripts/generate-css.ts` (`--gradient-brand`).
- [x] 2.2 Exponerlo como utilidad en `packages/tokens/src/tailwind-preset.ts` (vía `backgroundImage` o el mecanismo que corresponda al preset). Queda como `bg-gradient-brand`; se dejó documentado por qué no se abren los `bg-gradient-to-*` nativos de Tailwind.

## 3. Componentes

- [x] 3.1 `progress.tsx` — `SegmentedBar`: nueva opción para separar los segmentos. Con ella activa, el redondeo pasa a cada segmento y el contenedor deja de recortar (`overflow-hidden` + radio), porque si no las esquinas internas quedan comidas por el recorte del padre. Sin ella, el render actual no cambia. Prop `separated`; usa `gap-hug` (4px) y `rounded-pill` por segmento.
- [x] 3.2 `progress.tsx` — `Progress`: nueva opción de relleno de marca que usa el token de degradado en vez del color de severidad. Opt-in: sin ella, conserva verde dentro de rango y `danger` sobre 100. Prop `brandFill`, usa `bg-gradient-brand`.
- [x] 3.3 Actualizar los JSDoc de ambos componentes para describir las opciones nuevas y cuándo corresponde cada una (en `Progress`, dejar explícito que el degradado es decorativo y no comunica severidad). Se documentó además que con `brandFill` la barra deja de saturar a `danger`, que es la contrapartida de usarla.

## 4. Documentación

- [x] 4.1 Agregar un ejemplo en `apps/docs/src/examples/progress/` para `SegmentedBar` con segmentos separados. `05-barra-segmentada-separada.tsx`, mostrando separada y continua con los mismos datos para comparar.
- [x] 4.2 Agregar un ejemplo en `apps/docs/src/examples/progress/` para `Progress` con relleno de marca. `06-relleno-de-marca.tsx`, comparado contra el relleno por severidad.
- [x] 4.3 Actualizar `apps/docs/src/content/progress.tsx` (uso, anatomía y/o recomendaciones) para cubrir ambas opciones nuevas. Dos entradas en "cuándo usarlo" y dos en "cuándo no", incluida la advertencia de que `brandFill` desactiva la saturación a `danger`.
- [x] 4.4 Actualizar la página de fundamentos de tipografía (`apps/docs/src/content/fundamentos`) para mostrar el estilo de cifra y su rol dentro de la escala. El espécimen y la tabla derivan de `textStyle`, así que `metric` sale solo; se agregó su fila en `TEXT_STYLE_USE` y se amplió la prosa para explicar que la escala se nombra por rol y que `metric` vive en otro eje.

## 5. Verificación

- [x] 5.1 `pnpm --filter @tuya-ui/tokens build` y `pnpm --filter @tuya-ui/components build` sin errores. Ambos OK, más `tsc --noEmit` de docs limpio.
- [x] 5.2 Confirmar en el CSS generado que la utilidad del estilo de cifra y la del degradado existen con los valores esperados. `tokens.css` trae `--text-metric-*` (40px/40px/700/-0.04em) y `--gradient-brand`; `styles.css` trae `.bg-gradient-brand`. `.text-metric` no aparecía en el CSS de components porque ningún componente de tuip la usa. En su momento se dio por bueno verificando que el preset la expone (`fontSize.metric`), asumiendo que la Tailwind de la app la generaría — **asunción equivocada**: el `tailwind.config.js` del frontend no usa el preset de tuip, consume el `styles.css` ya construido. Corregido vía `safelist` en la tarea 1.5.
- [x] 5.3 Ver en el dev server de docs los cuatro estados: barra continua (default) vs separada, y progress por severidad (default) vs de marca — confirmando que los ejemplos existentes que no piden las opciones nuevas se ven igual que antes. Verificado midiendo estilos computados: la barra `separated` da `gap:4px`, `overflow:visible`, radio 0 en el contenedor y `9999px` por segmento; las otras tres barras de la página siguen en `gap:normal`, `overflow:hidden`, radio 8px y segmentos sin redondeo. `brandFill` da `linear-gradient(90deg, rgb(237,28,41), rgb(248,98,108))` y el default sigue verde sólido. **Hallazgo corregido**: la sección "Pesos" de la página de tipografía decía "Tres pesos, sin intermedios" y ya renderizaba cuatro — mismo tipo de desfase que el comentario de `typography.ts`; se reescribió.
- [x] 5.4 `pnpm run publish:local` desde la raíz de `tuip`, para que la app pueda consumir la versión nueva al retomar `add-people-dashboard-cards`. Empaquetado OK; verificado abriendo el `.tgz` de tokens que su `dist/tokens.css` ya trae `--text-metric-*` y `--gradient-brand` (no basta confiar en el log del pack).
