## 1. Componente Sidebar — estructura y tipos

- [x] 1.1 Crear `packages/components/src/sidebar.tsx`: elemento raíz `<nav aria-label={ariaLabel}>` (default `"Navegación principal"`), ancho `w-[248px]` expandido / `w-[64px]` colapsado — anchos exactos del mockup, sin equivalente en la escala de espaciado con alias, así que van como valores arbitrarios documentados en un comentario corto.
- [x] 1.2 Tipos: `SidebarNavItem` (`id`, `label`, `icon: ReactNode`, `href`, `badge?: number`), `SidebarNavGroup` (`label`, `items`), `SidebarDensity = "comfortable" | "compact"`, `SidebarProps` — cada prop propia con JSDoc inmediato encima. `Sidebar.displayName = "Sidebar"`.
- [x] 1.3 Cada grupo: encabezado (`text-label uppercase text-neutral-subtlest`, oculto vía `sr-only` — no `display:none` — cuando Sidebar está colapsado, para que siga etiquetando el `<ul>` vía `aria-labelledby` aunque no sea visible) seguido de un `<ul>` con un `<li>` por ítem.
- [x] 1.4 Cada ítem: `<a href={item.href}>` con `onClick` que siempre hace `preventDefault()` y llama `onNavigate(item.id, item.href)` — nunca navegación nativa sin intercepción, a diferencia de los enlaces de utilidad de Navbar. Alto `h-9` (`density="comfortable"`) o `h-8` (`density="compact"`).

## 2. Estado activo, contador y colapso visual

- [x] 2.1 Ítem activo (`item.id === activeId`): riel `border-l-2`, siempre presente (ancho reservado) para que activarse no desplace el contenido — color `border-l-transparent` inactivo / `border-l-brand-default` activo, mutuamente excluyentes; fondo `bg-neutral-default` (blanco), texto `font-semibold`, ícono en `text-neutral-default` en vez del `text-neutral-subtle` de un ítem inactivo. `aria-current="page"` en el `<a>`.
- [x] 2.2 Hover de un ítem inactivo: `hover:bg-neutral-subtle-hover`, sin cambio de peso ni desplazamiento. Hover de un ítem activo: `hover:opacity-80` en vez de cambio de fondo (ya está en blanco) — clases mutuamente excluyentes según `active`, no ambas presentes a la vez.
- [x] 2.3 Contador: `<span>` con `bg-brand-bold text-brand-on-bold`, formateado como `count > 99 ? "99+" : String(count)`; se omite por completo (no se renderiza en absoluto) cuando `badge` es `undefined` o `0`. Su valor se incorpora al `aria-label` del `<a>` del ítem (p. ej. `"Solicitudes, 12 pendientes"`), no solo a su apariencia.
- [x] 2.4 Colapsado: encabezados de grupo con `sr-only`, texto de cada ítem con `sr-only` (el `aria-label` ya lo cubre), ícono centrado. Envolver el `<a>` en `<Tooltip content={item.label} side="right">` — ya publicado, sin reimplementar — solo cuando Sidebar está colapsado.

## 3. Colapso: control, persistencia y auto-colapso por ancho

- [x] 3.1 Prop `collapsible` (default `true`): al pie, un botón con `Icon name="chevron-right"` rotado 180° cuando expandido (apunta a la izquierda, "colapsar") y sin rotar cuando colapsado (apunta a la derecha, "expandir"), texto "Colapsar"/"Expandir" visible solo cuando corresponde.
- [x] 3.2 Hook interno para el estado de colapso: lee `localStorage["tuya-ui:sidebar-collapsed"]` en el inicializador de `useState` con guarda `typeof window === "undefined"` y `try/catch`; solo lee/escribe esa clave cuando `collapsed` (prop) es `undefined`. Escribe en cada cambio no controlado; nunca cuando está controlado. Llama `onCollapsedChange` en ambos modos.
- [x] 3.3 Colapso automático: listener de `matchMedia("(max-width: 1119px)")` con el mismo patrón que `useNarrowViewport` de Navbar (reacciona a la transición del evento `change`, no a un chequeo continuo) — solo actúa cuando el colapso no está controlado desde afuera.

## 4. Registro

- [x] 4.1 Agregar entrada `sidebar` a `packages/components/registry/definitions.ts`: categoría `layout`, `status: "stable"`, `dependencies: ["utils", "icon", "tooltip"]`, `npmDependencies: ["react"]`.
- [x] 4.2 Agregar `export * from "./sidebar"` a `packages/components/src/index.ts`.
- [x] 4.3 Ejecutar `pnpm --filter @tuya-ui/components build` para regenerar `registry.json` y confirmar que Sidebar extrae props, peso y código fuente correctamente. 38 componentes; `sidebar: 1 exported component(s), 10 own prop(s)`.

## 5. Documentación

- [x] 5.1 Crear `apps/docs/src/content/sidebar.tsx` (uso, anatomía, accesibilidad) — Sidebar no es overlay ni `position: fixed`, así que la figura de anatomía puede usar el componente real dentro de un contenedor con alto fijo, igual que ActivityTimeline/Stepper/FileInput.
- [x] 5.2 Registrar `sidebarContent` en `apps/docs/src/content/index.ts` con la clave `"sidebar"`.
- [x] 5.3 Crear ejemplos en vivo en `apps/docs/src/examples/sidebar/*.tsx`: dos grupos con un ítem activo y un contador, variante `density="compact"`, y un ejemplo con `collapsed`/`onCollapsedChange` controlados desde afuera (mostrando que ese modo no toca `localStorage`). `npx tsc --noEmit` en `apps/docs` queda en limpio (los `size={18}` iniciales se corrigieron a `size={20}`, el único valor de `IconSize` que calza con la caja de ícono de 20×20px del componente).

## 6. Cierre

- [x] 6.1 Levantar el sitio de docs y verificar por teclado: Tab recorre los ítems en orden, Enter activa `onNavigate`, el ítem activo se anuncia como página actual (inspeccionar `aria-current` y el `aria-label` con el contador incluido). El enrutamiento de teclado por hardware (extensión de Chrome) resultó no confiable en esta sesión — mismo tipo de intermitencia ya documentada para capturas de pantalla. Verificado por vías equivalentes: el árbol de accesibilidad confirma los 6 ítems como `link` nativos en el orden correcto, sin `tabindex` que los saque del flujo; un click real (coordenadas, no ref) enfoca el `<a>` y deja `location.hash` sin cambios — confirma que el mismo `onClick` que llama `onNavigate` también hace `preventDefault()`; y como un `<a href>` nativo dispara ese mismo evento `click` tanto por mouse como por Enter (no hay una ruta de teclado separada en el componente), esa prueba cubre igual la activación por teclado. `aria-current="page"` y `aria-label="Solicitudes, 12 pendientes"` confirmados vía estilos computados sobre el DOM real.
- [x] 6.2 Verificar que el ítem activo se distingue por tres señales a la vez (riel, fondo, peso) — confirmado vía estilos computados sobre el ítem activo real: `border-left-color: rgb(237, 28, 41)` (vs. transparente inactivo), `background-color: rgb(255, 255, 255)` (vs. transparente), `font-weight: 600` (vs. 400) — las tres a la vez, y `border-left-width: 2px` constante en ambos estados (sin desplazamiento).
- [x] 6.3 Verificar colapso: activado el control, recargada la página completa (no SPA) con la extensión, confirmado que Sidebar aparece colapsado (o expandido, según el último valor) desde el primer render en ambas direcciones — prueba real de `localStorage`, no simulada. Repetido en modo controlado (ejemplo 03): con un valor centinela en `localStorage`, se interactuó con el toggle propio de la app y el Sidebar controlado respondió, pero `localStorage` permaneció exactamente en el centinela — confirma que el modo controlado nunca lo lee ni lo escribe.
- [x] 6.4 El redimensionado real del viewport (tanto la propia herramienta `resize_window` de la extensión como `MoveWindow` de Win32 directamente) no tuvo efecto en esta sesión — la ventana de Chrome permanece en su tamaño maximizado pase lo que pase, una limitación del entorno, no del componente. Verificado en su lugar interceptando `window.matchMedia` para devolver un objeto controlable solo para la consulta `"(max-width: 1119px)"`, forzando un remount real del Sidebar (navegación SPA de ida y vuelta) para que se suscribiera a través del parche, y disparando un evento `change` real: la instancia se colapsó y persistió en `localStorage`, confirmando el cableado del hook `useAutoCollapse`. Expandido de nuevo a mano después, se confirmó que permanece expandido (sin ser revertido) incluso tras un evento `resize` adicional y un re-render.
- [x] 6.5 Grep de valores hex/px sueltos en `sidebar.tsx` — limpio. Únicos hallazgos: los anchos `w-[248px]`/`w-[64px]` (documentados en 1.1) y el breakpoint `matchMedia("(max-width: 1119px)")` (documentado en 3.3), más comentarios; ningún color fuera de token.
- [x] 6.6 `pnpm lint`, `pnpm test` y `pnpm build` en la raíz del monorepo — los tres en verde. Registro con 38 componentes; `sidebar: 1 exported component(s), 10 own prop(s)`.
