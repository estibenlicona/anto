## Decisions

1. Sólo un passthrough: `separated` llega a `SegmentedBar`, que ya resuelve separación + `total` (gap entre piezas, pista neutra para el resto). Sin lógica nueva en CapacityBar.
2. Default `false`: el listado de Asignaciones y el detalle de célula siguen como están hasta que alguien lo pida.
