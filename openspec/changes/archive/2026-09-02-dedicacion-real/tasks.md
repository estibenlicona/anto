## 1. Contratos y mock

- [x] 1.1 Crear `features/dedication/services/dedicationService.ts`: `CapacityDedicationRowDto` (persona, célula e iniciativa, sprint en curso, puntos por etiqueta, FTE disponible, FTE asignado con su reparto Transformación / BAU, FTE comprometido, diferencia, lectura), `CapacityDedicationSummaryDto`, `CapacityDedicationListDto`, `CapacityDedicationDetailDto` (persona, asignación, sprints, sprint elegido con historias y actividad por día), `SprintSummaryDto`, `WorkItemDto` (`tag: "Initiative" | "Bau" | null`), `ActivityDayDto` (`date`, `commits`, `releases`, `features`), y los métodos `listCapacities(filters, page)`, `getCapacity(personId, sprint?)`, `syncAll()`, `syncCapacity(personId)` contra `/dedication/capacities…`.
- [x] 1.2 Agregar `pointsPerFtePerSprint` a `SprintConfig`, `useSprintConfig` (rango 1–100, rótulo "Puntos por FTE por sprint"), `sprint-config.handlers.ts` (26 por defecto, validación y snapshot `getPointsPerFtePerSprint()`), `AdminSprintsPage` (tercer campo y tarjeta "Dedicación real") y sus pruebas.
- [x] 1.3 Crear `mocks/handlers/dedication.seeds.ts`: por usuario de DevOps, siete sprints con fechas alrededor de `TODAY`, historias con etiqueta, puntos, estado, tablero y Epic, y actividad por fecha (commits, releases, features creadas); casos sembrados: en línea, por debajo, por encima, sin célula con puntos, sin identidad, historia sin etiqueta, sprint sin actividad.
- [x] 1.4 Crear `mocks/handlers/dedication.handlers.ts`: `GET /dedication/capacities` (filtros, paginación, resumen, puntos por FTE vigentes, última actualización), `GET /dedication/capacities/:personId` (con `sprint`), `POST /dedication/capacities/sync` y `POST /dedication/capacities/:personId/sync` (600 ms, `404`, `409`), derivando persona y FTE disponible, célula, iniciativa y dedicación de los otros mocks y calculando el FTE comprometido, la diferencia y la lectura con la función compartida; exponer `getCapacityReadingsSnapshot()` para la ficha y el badge; `reset`; registrar en el índice de handlers en lugar de `backlog.handlers`.
- [x] 1.5 En `personDetailService` y `personDetail.handlers.ts`: `devOpsIdentity` pasa a `{ id, userName, linkedAt, currentSprint }` con `currentSprint` (sprint, puntos, FTE comprometido, FTE asignado, FTE disponible, diferencia, lectura) tomado del snapshot de dedicación real (o `null` sin sprint); quitar `activeItems`, `initiativeItems`, `bauItems`, `pendingCuration` de DTO, adapter, semillas y fixtures.
- [x] 1.6 Pruebas de handler: listado con resumen y orden; filtros y paginación; detalle con y sin `sprint`; la lectura sigue a un cambio de asignación y a la tasa de puntos por FTE; sync `200` / `404` / `409`; vincular una identidad hace aparecer su dedicación real; el detalle de persona trae `currentSprint`.

## 2. Lectura compartida, barra única e icono

- [x] 2.1 Crear `features/dedication/adapters/dedicationReading.ts`: `readDedication({ points, initiativePoints, bauPoints, untaggedPoints, pointsPerFte, availableFte, dedicationPct, bauPct, transformationPct, hasIdentity, hasSprint })` → `{ committedFte, initiativeFte, bauFte, untaggedFte, assignedFte, assignedBauFte, assignedTransformationFte, deltaFte, reading }` (FTE con dos decimales; `reading`: `InLine | Below | Above | NoSquad | NoIdentity | NoSprint` con umbrales ±0.10 FTE) y `READING_LABELS` / `READING_VARIANTS` (En línea · éxito, Por debajo · info, Por encima · advertencia, Sin célula · neutro, Sin identidad · peligro en la ficha, Sin sprint · neutro); pruebas con los escenarios de la spec (0.69 / 0.80 → −0.11, 0.85 / 0.80 → +0.05, 0.77 / 0.50 → +0.27, 0.54 iniciativa · 0.12 BAU · 0.04 sin etiqueta, sin célula, cambio de tasa 26 → 30).
- [x] 2.2 Crear `features/dedication/components/DedicationBar.tsx`, la barra única, con la anatomía de `CapacityBar` de tuip: pista = FTE disponible, banda = FTE asignado, relleno segmentado por etiqueta (colores del mix; sin etiqueta en neutro) = FTE comprometido, cifras "0.69 / 0.80 FTE", exceso marcado cuando el comprometido supera el disponible; variantes `row`, `indicator` y `sprint`, y una variante de un solo color para comparar iniciativa con Transformación y BAU con BAU; sin barra cuando no hay identidad o sprint; `Tooltip` en la variante de fila; pruebas.
- [x] 2.3 Crear `features/dedication/components/ReadingIcon.tsx`: flecha arriba (advertencia) = Por encima, flecha abajo (info) = Por debajo, verificación (éxito) = En línea, guion (neutro) = Sin célula / Sin sprint, icono de DevOps apagado = Sin identidad; 20 px, `aria-label` y `Tooltip` con la lectura y la diferencia ("Por encima · +0.27 FTE"); pruebas.
- [x] 2.4 Crear `features/dedication/adapters/DedicationAdapter.ts` para las dos pantallas (etiquetas en español, formato de FTE con dos decimales y de diferencia con signo, fechas del sprint "17 ago – 30 ago", "Actualizado hace 12 min", filas del calendario de actividad por semana con nivel por total del día y detalle por tipo, "8 de 13 días con actividad", última actividad) y sus pruebas.

## 3. Listado de dedicación real

- [x] 3.1 Crear `useCapacityDedication` (lista con filtros, búsqueda con debounce, paginación, `refetch`) y `useDedicationSync` (todas y por persona, con estado de actualización y error).
- [x] 3.2 Crear `DedicationStatsCards` (Capacidades vinculadas con sin vincular, Por encima con nombres, Por debajo con nombres), con la anatomía de `PeopleStatsCards`.
- [x] 3.3 Crear `CapacitiesTable`: `Table` con toolbar (búsqueda, `FilterButton` Célula y Lectura, leyenda de la barra), columnas Capacidad · Célula e iniciativa · Sprint · Dedicación (`DedicationBar` de fila con "0.69 / 0.80 FTE" y "0.04 sin etiqueta" en advertencia cuando aplica) · Iniciativa (FTE y pts) · BAU (FTE y pts) · Lectura (`ReadingIcon` centrado, sin texto ni enlace); sin columnas de asignado, porcentajes ni actividad; esqueleto de carga, estado vacío y error; `PaginationBar`.
- [x] 3.4 Crear `DedicationContainer` en `/app/lead/dedicacion`: indicadores + tabla, acción "Actualizar desde Azure DevOps" y hora de última actualización publicadas en la franja del breadcrumb (`useLeadBreadcrumbActions`), toast al terminar y error en rol de peligro.
- [x] 3.5 Pruebas del contenedor y de los componentes con el mock real: orden por distancia, una fila leída en FTE, iconos de lectura por caso, filtros, fila sin identidad, actualizar todas.

## 4. Dedicación real de una capacidad

- [x] 4.1 Crear `CapacityDedicationContainer` en `/app/lead/dedicacion/:personId` con `useCapacityDedicationDetail(personId, sprint)` (sprint en la URL), breadcrumb `Plataforma / Dedicación real / <nombre>` y estados: sin identidad (estado vacío con acción a la ficha), sin sprints (estado vacío con actualizar), cargando y error.
- [x] 4.2 Crear `CapacityDedicationHeader`: vuelta al listado, avatar, nombre, cargo, célula con iniciativa activa y talla, FTE asignado sobre disponible; acciones Actualizar (secundaria), Ver ficha (neutra), Reasignar / Asignar a una célula (primaria, reutilizando `useReassignPerson` y `ReassignPersonDrawer` con refresco).
- [x] 4.3 Crear `SprintTabs` con `Tabs` de tuip (hasta siete, el en curso marcado) y `CapacitySprintStatsCards`: Dedicación (`DedicationBar` de indicador con "0.69 / 0.80 FTE", puntos como dato secundario, lectura como `Badge` y diferencia en FTE), Iniciativa y BAU (dos barras de un solo color: iniciativa frente a Transformación asignada y BAU frente a BAU asignado, con sus cifras y el FTE sin etiqueta en advertencia) y Actividad (total del sprint, commits, releases, features creadas y la última).
- [x] 4.4 Crear `DedicationBySprintPanel`: una fila por sprint con `DedicationBar` de sprint, cifras en FTE y lectura como `Badge`, el elegido resaltado y seleccionable con clic y teclado.
- [x] 4.5 Crear `SprintStoriesPanel`: tabla de historias (número, título truncado con título completo y debajo la etiqueta —con iniciativa mapeada o "Sin etiqueta" en advertencia— y el tablero, puntos, estado, enlace a DevOps) ordenada por puntos; estado vacío.
- [x] 4.6 Crear `ActivityCalendarPanel`: rejilla por semana (L–D) con celdas de cinco niveles por el total de actividad del día (neutro y acento `blue` al 25 / 50 / 75 / 100 %), fines de semana punteados, hoy con anillo, días por venir sin relleno, nombre accesible y `Tooltip` por celda con el detalle por tipo, leyenda, totales por tipo (commits, releases, features creadas), días con actividad y última actividad; desplazamiento horizontal interno para sprints largos; estado sin actividad.
- [x] 4.7 Pruebas: abrir con el sprint en curso, cambiar de sprint por pestaña y por fila, iniciativa y BAU frente a la asignación, historia sin etiqueta, mapa con actividad y sin actividad, reasignar refresca el FTE asignado, sin identidad y sin sprints.

## 5. Integración y limpieza

- [x] 5.1 Rutas y navegación: `dedicacion` y `dedicacion/:personId` en `routes.tsx` con redirección de `backlog` y `backlog/*`; entrada "Dedicación" (icono actual de Backlog) y título "Dedicación real" en `features/chapter-lead-shell/navigation.ts`; `useDedicationAttentionCount` (por encima + por debajo) en lugar de `useBacklogPendingCount` para el badge; pruebas de layout, navegación y rutas.
- [x] 5.2 Ficha de la persona: la tarjeta *Backlog en DevOps* pasa a *Dedicación real* con `DedicationBar` de indicador ("0.69 / 0.80 FTE"), la lectura del sprint en curso con la diferencia en FTE y "Ver dedicación" a `/app/lead/dedicacion/<id>`, "Sin sprint" cuando no hay, y el estado sin identidad como hoy; actualizar `PersonDetailStatsCards`, fixtures y pruebas.
- [x] 5.3 Borrar `features/backlog` entero (`BacklogQueue`, `CurrentStoryPanel`, `DecisionCards`, `RejectItemDrawer`, `BacklogDaySummary`, `backlogValidation`, `useBacklogPendingCount`, `backlogService`, pruebas y fixtures) y `mocks/handlers/backlog.*`, y cualquier referencia a `classify`, `skip`, `undo`, `reject`, `pendingCuration` o `BAU_CATEGORIES`.
- [x] 5.4 Actualizar en `context/docs/Roles_y_Permisos_Plataforma.md` la pantalla de Backlog (cola de triage y curación) por Dedicación real, y las funciones de clasificar / curar por leer la dedicación real y actualizar desde Azure DevOps.
- [x] 5.5 Correr el detector de *impeccable* (`node "<skill>/scripts/detect.mjs" --json frontend/src/features/dedication frontend/src/pages`) y corregir lo que reporte; `tsc`, lint, prettier y la suite completa; verificar en `pnpm dev:mock` el listado, la dedicación real de Carlos (Por debajo), de Valentina (Por encima), de Camila (sin identidad), la redirección de `/app/lead/backlog` y el calendario de Admin con tres campos.
