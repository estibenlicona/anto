# Tareas — Backend: módulo Capacidad (dedicación), modelo y lectura

## 1. Domain

- [x] 1.1 Crear los catálogos cerrados: `ValueObjects/SnapshotStatus.cs` (`Sealed`/`Provisional`/`Missing`), `BalanceSignal.cs` (`Usual`/`PossibleOverload`/`PossibleUnderload`/`NotEvaluable`), `SquadContext.cs` (`SameDirection`/`Different`/`NoSquad`), `NotEvaluableReason.cs` (`NoIdentity`/`NoSprint`/`MissingSnapshot`/`InsufficientHistory`), `EvidenceId.cs` (las seis evidencias, en el orden del contrato), `EvidenceDirection.cs` (`Over`/`Under`/`Neutral`/`Unknown`). Verificar con tests: valores en el orden del contrato para cada uno.
- [x] 1.2 Crear `Entities/Sprint.cs` (`AggregateRoot`): ctor `(string name, DateOnly startDate, DateOnly endDate, int holidays)` — nombre obligatorio y único (la unicidad la exige el use case, no el agregado), fin ≥ inicio, festivos ≥ 0. `IsCurrent(DateOnly today)` — nunca persistido, se deriva. Verificar con tests: fin anterior al inicio lanza, festivos negativos lanza, `IsCurrent` verdadero sólo dentro del rango.
- [x] 1.3 Crear las entidades poseídas de `SprintSnapshot`: `ConcurrentInitiativeSnapshot` (épica id/título, iniciativa id/nombre nulables, puntos), `WorkItemSnapshot` (id, número, título, tag nulable, épica/iniciativa nulables, puntos, estado, si entró después del inicio, tablero, url), `ActivityDaySnapshot` (fecha, commits, releases, features) — todas con ctor privado sin parámetros para EF. Verificar con tests: ctor público de cada una guarda los valores dados.
- [x] 1.4 Crear `Entities/SprintSnapshot.cs` (`AggregateRoot`): ctor `(Guid personId, Guid sprintId)` nace `Provisional`, sin ejecución. `SetExecution(committedAtStart, addedDuringSprint, completedPoints?, carryOverPoints?, wip?, otherUnavailableDays)`, `ReplaceInitiatives(...)`, `ReplaceWorkItems(...)`, `ReplaceActivity(...)`, `Seal(sealedAtUtc)` (400 si ya está `Sealed` o `Missing`), `MarkMissing()` (400 si ya está `Sealed`). Verificar con tests: nace `Provisional`; sellar dos veces lanza; sellar una `Missing` lanza; `committedPoints` derivado = comprometidos al inicio + agregados durante.
- [x] 1.5 `Person` gana `DevOpsUserId` (string, nulo) y `LinkDevOpsIdentity(string devOpsUserId)` — sin guardas de unicidad todavía (eso es de Detalle de persona cuando exista). Verificar con test: nace sin identidad, vincular la fija.
- [x] 1.6 Crear `Interfaces/ISprintRepository.cs` (`IRepository<Sprint>` más `GetByNameAsync(name)` y `GetAllAsync` ya ordenado por fecha de inicio) y `Interfaces/ISprintSnapshotRepository.cs` (`IRepository<SprintSnapshot>` más `GetByPersonAndSprintAsync(personId, sprintId)`, `GetByPersonAsync(personId)` y `GetByPersonIdsAsync(personIds)` para la mediana de célula). Verificar: build.

## 2. Application — cálculo (FTE, referencia, señal)

- [x] 2.1 Crear `Dedication/CapacityCalculator.cs`: puerto literal de `capacityFte.ts` — FTE disponible = contractual × (hábiles del sprint − festivos − vacaciones − ausencias − otras indisponibilidades) / hábiles, acotado a `[0, contractual]`; vacaciones/ausencias vía `IAbsenceRepository` recortadas contra las fechas del sprint con `BusinessDayMath.ClampRange`, admitiendo medias jornadas; horas disponibles/descontadas calculadas siempre, nunca guardadas. Verificar con tests: los mismos números de los escenarios del spec (10 hábiles, 1 festivo, 1 ausencia → 0.80; colaborador 0.5 sin descuentos → 0.50; media jornada descuenta 0.5).
- [x] 2.2 Crear `Dedication/HistoryReference.cs`: puerto literal de `history.ts` — `Median` (no promedio, con desempate de par), `SealedSprints` (excluye el sprint actual y los no sellados, recorta a la ventana), `BuildReference` (mediana propia, mediana de célula como SP por colaborador de esa célula por sprint sellado —ver design.md—, suficiencia contra el mínimo, desviaciones propia y de célula). Verificar con tests: mediana de longitud par promedia los dos centrales; ventana insuficiente marca `sufficient=false`; mediana de célula agregada por colaborador, no por SP crudo.
- [x] 2.3 Crear `Dedication/BalanceSignalCalculator.cs`: puerto literal de `balanceSignal.ts` — las constantes de umbral con su nombre (`DemandDeviationPct=25/50`, `CompletionDropPp=15/30`, `CarryOverExcessPp=10/25`, `UnplannedRatePct=20/50`, `MultitaskingInitiatives=3/5`, `MultitaskingWip=4/6`, `SquadSameDirectionTolerancePp=10`, `StrongMinEvidences=2`), las seis funciones de evaluación, `ResolveSquadContext` y `Aggregate`. Verificar con tests: cada escenario del spec (balanceado, una evidencia no basta, una fuerte ya alcanza, dos concordantes, direcciones opuestas caen a habitual, célula en la misma dirección no cambia la señal), y el caso de cumplimiento sin sellar que sólo evalúa el 100 %.

## 3. Application — DTOs, contexto y use cases

- [x] 3.1 DTOs del contrato en `DataTransferObjects/DedicationDtos.cs`: todos los de `CollaboratorDedicationListDto`/`CollaboratorDedicationRowDto`/`CollaboratorDedicationDetailDto` y sus anidados (`DedicationPersonDto`, `DedicationAllocationDto`, `SprintRefDto`, `CapacityDto`/`CapacityBreakdownDto`, `SprintExecutionDto`, `ReferenceDto`, `MultitaskingDto`/`ConcurrentInitiativeDto`, `BalanceSignalDto`/`BalanceEvidenceDto`, `UnplannedWorkDto`, `WorkItemDto`, `ActivityDayDto`, `SprintTrendPointDto`/`SelectedSprintDto`, `SprintActivityTotalsDto`, `ListSprintDto`, `DedicationSettingsDto`, `CollaboratorDedicationSummaryDto`). Verificar: un test que serializa a JSON y comprueba los nombres exactos del contrato (`hasIdentity`, `contractualFte`, `availableFte`, `ownMedian`, `squadDeviationRate`, `overCount`, `notEvaluableReason`, `addedAfterSprintStart`, `snapshotStatus`, `sealedAt`).
- [x] 3.2 Crear `Dedication/DedicationContext.cs`: arma, para una persona y un sprint elegido, `CapacityDto` (vía `CapacityCalculator`), `SprintExecutionDto`, `UnplannedWorkDto`, `MultitaskingDto`, `ReferenceDto` (vía `HistoryReference`, usando `ISprintSnapshotRepository.GetByPersonAsync` y `GetByPersonIdsAsync` de la célula) y `BalanceSignalDto` (vía `BalanceSignalCalculator`), resolviendo primero el motivo de "no evaluable" (sin identidad, sin sprint para esa persona, snapshot `Missing`, histórico insuficiente) antes de calcular nada más. Verificar con tests: colaborador sin identidad → `NotEvaluable(NoIdentity)` sin cifras inventadas; colaborador con snapshot sellado y brecha → señal y evidencias coherentes con 2.3.
- [x] 3.3 `GetCollaboratorsUseCase` (`GET /dedication/collaborators`): resuelve el sprint (vigente si no se indica; 400 si el nombre no existe), arma una fila por persona del chapter con `DedicationContext`, calcula el resumen de los cuatro indicadores (con quiénes, cuando son pocos), ordena por señal accionable primero (sobre/sub, luego habitual, luego no evaluables), filtra por búsqueda y por célula, pagina. Verificar con tests: orden por señal; filtro por célula; resumen cuenta cada motivo de no evaluable por separado; sin colaboradores responde vacío con el resumen en cero.
- [x] 3.4 `GetCollaboratorDetailUseCase` (`GET /dedication/collaborators/{personId}`): 404 si la persona no existe; arma la cabecera del sprint elegido (capacidad, ejecución, referencia, foco, balance, historias y actividad del snapshot) y la tendencia (los sprints de la ventana con su ejecución y actividad, marcando los sin snapshot). Verificar con tests: persona inexistente lanza 404; tendencia excluye el sprint actual y ordena de más antiguo a más reciente; sprint elegido sin snapshot responde con `selectedSprint` en `Missing` y sin cifras de ejecución.
- [x] 3.5 Registrar ambos use cases y sus calculadoras en `Application/DependencyInjection` bajo `// Capacidad (dedicación)`. Verificar: build y el test de DI que resuelve el contenedor.

## 4. Infrastructure

- [x] 4.1 `SprintConfiguration` (tabla `Sprints`; índice único por `Name`) y `SprintRepository`. Verificar con un test sobre Sqlite en memoria: guardar y releer un sprint por nombre.
- [x] 4.2 `SprintSnapshotConfiguration` (tabla `SprintSnapshots`; `Status` con converter a string; tres colecciones poseídas —iniciativas, historias, actividad— cada una con clave shadow; índice por `(PersonId, SprintId)`) y `SprintSnapshotRepository`. Columna nueva `DevOpsUserId` en la configuración existente de `Person`. `DbSet`s en `ApplicationDbContext`, migración nueva y registro en DI. Verificar con un test: guardar un snapshot sellado con sus tres colecciones, releerlo y obtener los mismos valores.
- [x] 4.3 Semillas: siete sprints tipo `S12`…`S18` (dos semanas cada uno, `S18` conteniendo la fecha de hoy), con snapshots que cubran cada señal y cada motivo de "no evaluable" — al menos: un colaborador en carga habitual, uno en posible sobreasignación, uno en posible subasignación, uno cuya célula se desvía igual (`SameDirection`), uno sin identidad DevOps, uno con histórico insuficiente, uno con un sprint sin snapshot (`Missing`) dentro de la ventana. Verificar: `dotnet run` y `GET /dedication/collaborators` devuelve las cuatro señales representadas en el resumen.

## 5. WebApi y contrato

- [x] 5.1 `DedicationEndpoints`: las 2 rutas de lectura (`GET /dedication/collaborators` con `sprint`/`page`/`pageSize`/`search`/`squadId`; `GET /dedication/collaborators/{personId}` con `sprint`); `Produces` con 200 y 404 (detalle) / 400 (sprint inválido, ver design.md). Verificar: Swagger lista las 2 operaciones bajo `/api/v1/dedication`.
- [x] 5.2 `Swagger/Examples/DedicationExamples.cs` con una fila en posible sobreasignación (capacidad con descuento, demanda fuera de tolerancia, foco de dos iniciativas) y un detalle con tendencia de varios sprints, uno de ellos sin snapshot. Verificar: Swagger muestra los ejemplos.

## 6. Verificación

- [x] 6.1 `dotnet build` + `dotnet test` en verde (sólo las fallas pre-existentes de `RestClientBehaviorTests`).
- [x] 6.2 Smoke con Postgres local (`curl -sk`): `GET /dedication/collaborators` sin `sprint` resuelve el vigente y trae las cuatro señales sembradas en el resumen; con `sprint` inválido → 400; filtro por célula reduce el listado y no cambia el resumen; `GET /dedication/collaborators/{id}` de una persona sin identidad → señal "No evaluable" sin cifras; de una con histórico suficiente → señal, evidencias y tendencia coherentes; persona inexistente → 404.
- [x] 6.3 `backend/ENDPOINTS.md`: las dos filas de lectura de Capacidad (dedicación) a 🟢, con nota de que la sincronización sigue 🔴 hasta el segundo cambio; total del resumen actualizado.
