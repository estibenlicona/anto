## 1. entra-local — app roles de usuario y asignaciones

- [x] 1.1 En `C:\Repos\entra-local`, crear la rama `feature/user-app-roles` desde `main` y agregar `src/store/migrations/migration-003-app-role-assignments.ts` (tabla `app_role_assignments`: `id`, `app_id`, `role_id`, `principal_type` `user|group`, `principal_id`, `created_at`; único `(role_id, principal_type, principal_id)`; cascada al borrar rol, usuario o grupo) registrada en `migrations/index.ts`. Verificar: `pnpm test` sigue en verde y una base existente abre sin error tras la migración (test de migraciones).
- [x] 1.2 Tipos y repositorio (`store/types.ts`, `store/repositories/apps.ts`): `AppRoleAssignment`, `addAssignment`, `listAssignments(appId)`, `removeAssignment`, y `rolesForUser(appId, userId)` (directas ∪ por grupos, sólo habilitados, sin duplicados, ordenadas por valor). Verificar con tests unitarios: directa + grupo del mismo rol → un solo valor; rol deshabilitado excluido; borrar el grupo elimina la asignación.
- [x] 1.3 Admin API (`admin/schemas.ts`, `dto.ts`, `routes.apps.ts`): `roleCreateSchema`/`rolePatchSchema` aceptan `allowedMemberTypes` con `User` y/o `Application`; rutas `GET/POST /api/apps/:id/roles/:roleId/assignments`, `DELETE …/assignments/:assignmentId`, `GET /api/apps/:id/assignments` (con `roleValue` y `principalDisplayName`). Verificar con tests de integración: 201 al asignar usuario y grupo; 400 si el rol no admite `User`; 404 principal inexistente; 409 duplicado; 204 al retirar.
- [x] 1.4 Claims: `tokenConfig.ts::resolveAppTokenClaims` agrega `roles` para tokens de usuario (ID token → app cliente; access delegado → app recurso registrada; omitido si vacío o si el recurso es Graph); `response.ts` y `service.ts` (preview) pasan la app correcta según `kind`; el flujo app-only no se toca. Verificar con tests: ID token con `roles: ["Plataforma.Admin"]`; access token para `api://<api>` con los roles de la API y no del cliente; sin asignaciones no hay claim; preview y token emitido coinciden; e2e de client credentials intacto.
- [x] 1.5 Portal (`portal/src/routes/AppDetail.tsx` + `portal/src/api`): en la sección "App roles", tabla de asignaciones por rol con selector de principal (usuarios y grupos) y borrado por fila; el alta de rol permite elegir tipos de miembro. Verificar: `pnpm typecheck`, test de `AppDetail` cubre listar/asignar/quitar, y `pnpm build:portal` termina.
- [x] 1.6 Convenciones del proyecto: `specs/2026-09-06_29-user-app-roles.md` (alcance, modelo, API, claims, criterios de aceptación) y entrada en `memory/decisions.md` con el formato del repo; `pnpm lint` y `pnpm test` en verde. Verificar: los dos archivos existen y lint/test terminan en 0.
- [x] 1.7 Reiniciar el emulador desde la rama (`pnpm dev`, puerto 8443) y comprobar con `curl -k` que `GET /admin/api/health` responde y que `POST /api/apps/:id/roles` acepta `allowedMemberTypes: ["User"]`. Verificar: ambas respuestas 2xx.

## 2. Máquina de desarrollo

- [x] 2.1 Confiar el certificado del emulador: en `entra-local`, `pnpm start -- trust --apply` (o el comando que imprime `pnpm start -- trust`) y confirmar con `pnpm start -- show-cert` + `certutil -user -store Root` que el certificado `entra.localhost` está en el almacén raíz del usuario; `hosts` ya resuelve `*.entra.localhost` (no tocar). Verificar: Chrome abre `https://login.entra.localhost:8443/` sin aviso de certificado.

## 3. Host — semilla del directorio

- [x] 3.1 `host/scripts/entra-local-seed.mjs` + script `entra:seed` en `package.json`: contra `ENTRA_LOCAL_URL` (default `https://localhost:8443`), crea o reutiliza la app "Plataforma Host" (SPA, redirect `http://localhost:4400/`), roles `Plataforma.Admin`/`Plataforma.ChapterLead`/`Plataforma.TechLead` (`User`), personas `ana.admin`, `tomas.giraldo`, `lucia.tecnica`, `rita.restringida` (`@tuya.local`) y sus asignaciones; TLS relajado sólo en el proceso y sólo para loopback/`*.entra.localhost`; imprime el bloque para `.env.development.local`; error claro si el emulador no responde. Verificar: dos ejecuciones seguidas terminan en 0 y `GET /admin/api/apps`, `/users` y `/api/apps/:id/assignments` muestran las mismas cantidades después de la segunda.
- [x] 3.2 Escribir `host/.env.development.local` (ignorado por git) con el bloque impreso: `VITE_ENTRA_CLIENT_ID`, `VITE_ENTRA_TENANT_ID`, `VITE_ENTRA_AUTHORITY=https://login.entra.localhost:8443/<tenant>`, `VITE_ENTRA_KNOWN_AUTHORITIES=login.entra.localhost:8443`, `VITE_ENTRA_REDIRECT_URI=http://localhost:4400/`, `VITE_ENTRA_API_SCOPES=` vacío. Verificar: `git status` no lo lista.

## 4. Host — autoridad configurable y tercer rol

- [x] 4.1 `features/entra/entraConfig.ts` + `vite-env.d.ts` + `.env.example`: `VITE_ENTRA_AUTHORITY` (opcional; default nube + tenant) y `VITE_ENTRA_KNOWN_AUTHORITIES` (opcional; si falta y la autoridad es explícita, el host de su URL); `createMsalInstance` pasa `auth.knownAuthorities`. Verificar con tests: sin las variables → autoridad `login.microsoftonline.com/<tenant>` y `knownAuthorities` vacío; con autoridad explícita y sin conocidas → `["login.entra.localhost:8443"]`; con ambas → las declaradas.
- [x] 4.2 Rol líder técnico: `APP_ROLES` suma `tech-lead`, `ENTRA_ROLE_TO_APP_ROLE` suma `Plataforma.TechLead`, `ROLE_LABELS` suma "Líder Técnico". Verificar con tests: `mapEntraRoles(["Plataforma.TechLead"])` → `["tech-lead"]`; `navbarUserOf` muestra la etiqueta.
- [x] 4.3 Registro de módulos: `iniciativas` ("Iniciativas y Células", `/iniciativas`, `admin`/`chapter-lead`/`tech-lead`, color propio); `capacidad` sigue con `admin`/`chapter-lead`. Verificar con tests: tech-lead ve sólo Iniciativas y Células; chapter-lead ve los dos; `/iniciativas/x` con tech-lead muestra el placeholder y `/capacidad` con tech-lead muestra "Sin permisos".

## 5. Host — retirar el simulador

- [x] 5.1 Borrar `src/dev/`, el script `dev:auth`, `cross-env` si ya no se usa, `VITE_AUTH_SIMULATOR` de `vite-env.d.ts`, `.env.example`, `vite.config.ts` (`define`) y README; `main.tsx` queda sólo con `readEntraConfig` → `EntraAuthProvider` y `ConfigErrorPage` ante `EntraConfigError`; `App` pierde `DevPanel`. Verificar: `grep -r "simulator\|VITE_AUTH_SIMULATOR" host/src host/*.ts host/*.json host/README.md` no devuelve nada; `pnpm typecheck`, `pnpm lint`, `pnpm test` (cobertura ≥ 80 %) y `pnpm build:prod` en 0.

## 6. Verificación de extremo a extremo

- [x] 6.1 Smoke con el emulador (`pnpm dev` en 4400, o `vite build --mode development` + `vite preview` si Chrome congela las capturas): Ana (admin) → redirect al emulador → selector de cuenta → regreso a la ruta pedida → portal con los dos módulos y "Administrador/a" en la cuenta; Tomás (líder de expertise) → los dos módulos; Lucía (líder técnica) → sólo Iniciativas y Células, `/capacidad` → "Sin permisos"; Rita → portal vacío; recarga mantiene la sesión sin volver al emulador; "Cerrar sesión" → logout del emulador → pantalla de inicio de sesión del host. Verificar: cada paso como se describe y el ID token (claims en la sesión) trae `roles` con el valor esperado.
- [x] 6.2 `host/README.md`: sección "Correrlo" con los tres pasos (emulador + `trust`, `pnpm entra:seed`, `pnpm dev`), tabla de variables con `VITE_ENTRA_AUTHORITY`/`VITE_ENTRA_KNOWN_AUTHORITIES`, roles con `tech-lead`, personas de prueba, y la nota de que contra Entra real basta con omitir las dos variables de autoridad. Verificar: el README no menciona el simulador y cubre esos puntos.
