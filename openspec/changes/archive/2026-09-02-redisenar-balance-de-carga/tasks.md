> **Nota de secuencia.** Retirar `Review` del tipo `BalanceSignal` en 1.1 rompe
> a propósito las cuatro superficies que lo leen, y agregar campos al contrato en
> el grupo 2 rompe el adapter y los componentes hasta que los grupos 4 a 6 los
> reconstruyen. `tsc` y la suite completa **no pueden estar en verde entre 1.1 y
> 6.3**; son puertas de fin de grupo, no de cada tarea. Donde una tarea dice
> "verificar con `tsc`", léase "sin errores nuevos fuera de los módulos que este
> change reescribe". La puerta real de árbol limpio es 7.4.

## 1. Reglas de la señal

- [x] 1.1 En `features/dedication/services/dedicationService.ts` y `adapters/balanceSignal.ts`: retirar `"Review"` de `BalanceSignal`, renombrar `"Balanced"` → `"Usual"`, actualizar `BALANCE_SIGNALS` al orden de presentación de cuatro valores, y actualizar `SIGNAL_LABELS` ("Carga habitual"), `SIGNAL_PHRASES` ("Los indicadores están dentro de la tolerancia de sus señales.") y `SIGNAL_VARIANTS` (`PossibleUnderload` pasa de `info` a `warning`). Verificar con `tsc`, que enumera cada superficie por actualizar, y con `grep -rn "\"Review\"\|Balanced" src/` sin resultados fuera de los archivos de los grupos 4 a 6.
- [x] 1.2 Colapsar la agregación en `balanceSignal.ts`: `STRONG_MIN_EVIDENCES` pasa de 3 a 2, se retira `REVIEW_MIN_EVIDENCES`, y `aggregate` devuelve `Usual` con 0 evidencias o 1 no fuerte, la señal accionable de la dirección con 1 fuerte o ≥ 2 concordantes, y `Usual` cuando hay evidencias en las dos direcciones. Verificar con las pruebas de 1.4.
- [x] 1.3 Quitar el descuento del modificador de célula: `squadContext` se sigue calculando con `resolveSquadContext` y viaja en el DTO, pero `SameDirection` ya no resta del conteo. Verificar con una prueba de que dos colaboradores con tres evidencias `under` y célula que los acompaña conservan `underCount: 3` y señal `PossibleUnderload`.
- [x] 1.4 Reescribir `adapters/__test__/balanceSignal.test.ts` con los escenarios de la spec: carga habitual con las seis evidencias neutras; una evidencia no fuerte sigue en habitual; una evidencia fuerte ya es accionable; dos concordantes son accionables; cuatro `over` dan sobreasignación; tres `under` dan subasignación; la célula que acompaña anota sin cambiar el conteo ni la señal; evidencias opuestas caen a habitual; los cuatro motivos de no evaluable; y que ninguna entrada produce "Review". Verificar con la suite del módulo en verde.

## 2. Contrato y parámetro del calendario

- [x] 2.1 En `SprintConfig`, `sprintConfigService`, `sprint-config.handlers.ts` y `AdminSprintsPage`: agregar `hoursPerSprint` (80 por defecto, entre 20 y 400) con su rótulo *Horas por sprint* y su ayuda, su validación de rango en formulario y handler (`400`), y la tarjeta de qué usa el calendario nombrando también las horas. `getDedicationSettings()` lo expone. Verificar con las pruebas de `useSprintConfig`, `sprintConfigService` y `AdminSprintsPage` actualizadas en verde, incluida una que rechaza un valor fuera de rango.
- [x] 2.2 En `dedicationService.ts`: `CapacityDto` gana `availableHours` y `deductedHours`; `ReferenceDto` gana la desviación ya formateable si hace falta; `CollaboratorDedicationListDto` gana el sprint elegido (`sprint`, `isCurrent`, `hasPrevious`, `hasNext`) y `hoursPerSprint` en `settings`; `CollaboratorDedicationSummaryDto` reemplaza `review` y `balanced` por `usual`; `CollaboratorDedicationRowDto` gana las iniciativas del sprint; `CollaboratorDedicationFilters` pierde `signals`; `listCollaborators` acepta `sprint`. Verificar con `tsc` y con `grep -rn "signals\|review:" src/features/dedication/services/` sin resultados.

## 3. Mock

- [x] 3.1 En `dedication.handlers.ts`: aceptar `sprint` en `GET /dedication/collaborators`, resolver el sprint pedido contra los sprints del chapter (el en curso por defecto, y cayendo al en curso cuando no existe), y calcular todas las filas para ese sprint. La respuesta declara el sprint elegido, si es el en curso y si hay anterior y siguiente. Verificar con las pruebas de 3.4.
- [x] 3.2 En `dedication.handlers.ts`: calcular `availableHours` y `deductedHours` desde `hoursPerSprint`, exponer por fila las iniciativas que las historias del sprint tocaron, retirar el filtro por `signal` y cambiar el resumen a los cuatro contadores con el desglose de motivos de no evaluable. Verificar con las pruebas de 3.4.
- [x] 3.3 Ajustar `dedication.seeds.ts` para los casos que el rediseño necesita ver: dos colaboradores de una misma célula con subasignación y célula que los acompaña —que ahora cuentan los dos en el indicador—, y uno que toca más de dos iniciativas en el sprint en curso para ejercitar el "+N". Verificar con la prueba de semillas actualizada, que enumera los casos.
- [x] 3.4 Probar el handler: listado del sprint en curso y de un sprint anterior con el mismo resumen coherente; los vecinos del sprint declarados y sin siguiente en el en curso; un `sprint` desconocido cae al en curso; las horas siguen a `hoursPerSprint` sin mover la señal; el resumen cuenta cuatro señales y ninguna es "revisar"; el filtro por `signal` ya no filtra; las iniciativas del sprint viajan por fila; y los dos colaboradores de la célula baja cuentan los dos en subasignación con su marca de contexto. Verificar con la suite de handlers en verde.
- [x] 3.5 En `personDetail.handlers.ts` y `personDetailService`: `currentSprint` gana la desviación frente al habitual y las horas de la capacidad, y su señal es una de las cuatro. Verificar con las pruebas de `personDetail` actualizadas en verde y una que compara la señal de la ficha con la fila del listado.

## 4. Componentes compartidos

- [x] 4.1 `BalanceSignalIcon` y `BalanceSignalBadge` con cuatro valores: verificación en éxito para *Carga habitual*, `trend-up` en peligro para sobreasignación, `trend-down` en **advertencia** para subasignación, guion neutro para no evaluable; y el `Badge` acompañado de la anotación de contexto de célula cuando aplica. Verificar con pruebas de los cuatro valores, de que el icono no renderiza texto visible, y de que la anotación aparece sólo con `SameDirection`.
- [x] 4.2 `CapacityFteBar` muestra las horas debajo de las cifras ("72 h · −8 h por ausencias", sin la resta cuando es cero). Verificar con pruebas de un sprint con descuentos, uno sin descuentos y un colaborador a tiempo parcial.
- [x] 4.3 `DemandBar` colorea el relleno con el rol de la señal, conserva la marca del habitual, retira la marca de célula en la variante de fila y expone la desviación junto a las cifras. Verificar con pruebas de las tres señales de color, de que la variante de fila no dibuja la marca de célula y de que sin histórico sólo muestra la cifra.
- [x] 4.4 En `DedicationAdapter.ts`: formato de horas, de desviación en la fila ("+36 %") y del pie de tolerancia ("Tolerancia ±25 %" / "Dentro de la tolerancia"), rótulo de multitarea en HUs abiertas, iniciativas del sprint con su colapso en "+N", y el vocabulario de cuatro señales. Verificar con las pruebas del adapter en verde y `grep -rn "WIP" src/features/dedication/components/` sin resultados.

## 5. Listado

- [x] 5.1 Crear `SprintNavigator` para la franja del breadcrumb: sprint anterior, nombre y fechas del elegido, siguiente, con la marca "En curso" y el avance deshabilitado en el sprint en curso. Verificar con pruebas de los tres estados (con vecinos, en el en curso, sin sprints).
- [x] 5.2 En `useCollaboratorDedication`: el sprint elegido vive en la URL, se pasa al servicio, y se retira el estado de filtro por señal. Verificar con pruebas del hook contra el mock: cambiar de sprint recarga las filas, el sprint queda en la URL, y ya no existe `onSignalsChange`.
- [x] 5.3 `BalanceStatsCards` con las cuatro tarjetas —sobreasignación, subasignación, carga habitual, no evaluables— cada una con su punto de color, sus nombres cuando son pocos y el desglose de motivos. Verificar con pruebas de conteos, del punto por rol y del desglose.
- [x] 5.4 `CollaboratorsTable` con las seis columnas del artefacto (Colaborador · Célula e iniciativas · Capacidad · Demanda vs habitual · Multitarea · Balance), la toolbar con búsqueda, marcas de célula seleccionables y el conteo "N de M personas", y sin columna de Sprint ni filtro por señal. Verificar con pruebas de que no existen columnas de Sprint, de FTE comprometido, de FTE asignado ni de porcentajes, y de que la toolbar no ofrece filtro por señal.
- [x] 5.5 `DedicationContainer` publica el navegador de sprint junto a la hora y la acción de actualizar en la franja, y pasa el sprint elegido a la tabla y a los indicadores. Verificar con una prueba de contenedor que renderiza navegador + indicadores + tabla y dispara la actualización.
- [x] 5.6 Probar el listado extremo a extremo contra el mock: orden por señal accionable, la fila de sobreasignación leída completa (capacidad con horas, demanda con desviación y tolerancia, multitarea en HUs abiertas, icono de señal), una fila dentro de la tolerancia, los cuatro iconos, retroceder un sprint mueve indicadores y filas, filtrar por célula con las marcas, y la fila no evaluable con su motivo. Verificar con la suite del feature en verde.

## 6. Dashboard, ficha y badge

- [x] 6.1 Dashboard: la tarjeta de Capacidad muestra las horas, la de Demanda su desviación, la de Balance la señal de cuatro valores con la anotación de célula; `WhyThisSignalPanel` y `ReferencePanel` dicen "contexto de célula" en vez de descuento; `MultitaskingPanel` dice HUs abiertas. Verificar con las pruebas del contenedor actualizadas: las cuatro tarjetas, el panel de evidencias con la anotación, y que ninguna cadena dice "Revisar".
- [x] 6.2 Ficha de la persona: el indicador *Dedicación real* muestra la señal de cuatro valores, "28 SP · habitual 22 · +27 %" y la capacidad en FTE y horas. Verificar con las pruebas de `PersonDetailStatsCards`, adapter y fixtures actualizadas en verde.
- [x] 6.3 `useDedicationAttentionCount` cuenta sobre el resumen nuevo (`possibleOverload + possibleUnderload`) y el badge sigue apareciendo sólo con señales accionables. Verificar con las pruebas de layout: el badge cuenta las dos señales y desaparece cuando todos leen Carga habitual o No evaluable.

## 7. Documentación y cierre

- [x] 7.1 Actualizar `context/docs/Roles_y_Permisos_Plataforma.md` con el vocabulario nuevo: cuatro señales, tolerancia, capacidad en horas y navegador de sprint. Verificar leyendo el doc: no queda ninguna mención a la señal "Revisar" ni a "Balanceado".
- [x] 7.2 Anotar en la cabecera de `dedicationService.ts` qué debe implementar el backend real sobre lo ya escrito: que `hoursPerSprint` es un parámetro de lectura y nunca un parte de trabajo, y que el listado responde un sprint a la vez. Verificar que el comentario existe y nombra los dos puntos.
- [x] 7.3 Revisar que el vocabulario retirado no sobreviva en ninguna superficie: `grep -rni "revisar\|balanceado\|WIP" src/features src/mocks` sólo devuelve usos legítimos (el campo `wip` del contrato y los textos que no hablan de la señal). Verificar con el grep y con la prueba de lenguaje de interfaz en verde.
- [x] 7.4 Correr `tsc`, lint, prettier y la suite completa, y verificar en `pnpm dev:mock` contra el artefacto: los cuatro indicadores con su punto, el navegador de sprint moviendo el listado entero, una fila de sobreasignación con sus horas y su tolerancia, una dentro de la tolerancia, los dos subasignados de la misma célula contados en su indicador, las marcas de célula filtrando, y el Calendario de sprints con sus seis campos.
