# Tareas — Backend: módulo Prefacturación

## 1. Domain

- [x] 1.1 Crear `ValueObjects/BillingStatus.cs` (catálogo cerrado: `Pending`/Pendiente, `Received`/Recibida, `InReview`/EnRevisión, `Approved`/Aprobada, `Objected`/Objetada — **sin `None`**, que es un estado sintético de respuesta y nunca se persiste), `ValueObjects/AdjustmentReason.cs` (`Overtime`, `PartialEntry`, `Exit`, `Other`) y `ValueObjects/Currency.cs` (sólo `COP`). Verificar con tests de cada VO: valores en el orden del contrato y `From` inválido lanza listando los válidos.
- [x] 1.2 Crear los value objects de detalle: `BillingAdjustment` (monto entero no cero, motivo, nota — nota `""` si no llega), `PrefactureDocument` (número no vacío, fecha, monto > 0 redondeado, moneda, imputación), `Imputation` (7 campos texto nulables, `""` normalizado a `null`), `Objection` (motivo no vacío, fecha), `AbsenceDiscount` (días hábiles, monto). Verificar con tests: monto de ajuste en cero o decimal lanza, número de documento vacío lanza, imputación con cadena vacía guarda `null`.
- [x] 1.3 Crear `Entities/Prefacture.cs` (`AggregateRoot`): ctor de generación (personId, snapshot congelado de nombre/cargo/célula/proveedor/costo mensual, período) que nace `Pending`. Verificar con tests: nace Pending con el snapshot dado, período con forma inválida lanza.
- [x] 1.4 `Prefacture.RegisterDocument(document)`: 400 si está `Approved`; 400 si ya tiene documento y no está `Objected`; si pasa, guarda el documento, fija `Prefactured = document.Amount` y pasa a `Received` (aunque venga de `Objected`). Verificar con tests: sobre `Pending` queda `Received`; sobre `Objected` reemplaza el documento y vuelve a `Received` sin limpiar la objeción anterior; sobre una que ya tiene documento y no está objetada, lanza; sobre `Approved`, lanza.
- [x] 1.5 `Prefacture.SetPrefactured(amount)` y `Prefacture.SetAdjustment(adjustment)`/`ClearAdjustment()`: 400 si `Approved` u `Objected`; `SetPrefactured` exige documento ya registrado; los tres comparten la regla `MoveToReview` (si estaba `Received`, pasa a `InReview`; si no, no cambia el estado). Verificar con tests: ajustar una `Received` la deja `InReview`; ajustar una `InReview` la deja `InReview`; cualquiera de los tres sobre `Approved`/`Objected` lanza; `SetPrefactured` sin documento lanza.
- [x] 1.6 `Prefacture.Approve(discount, note)` y `Prefacture.Object(reason)`: ambas sólo desde `Received`/`InReview` (lanzan si no); `Approve` exige `note` no vacía si la diferencia (calculada con el `discount` recibido) es distinta de cero, congela `FrozenDiscount = discount` y fija `ApprovedAtUtc`; `Object` exige `reason` no vacío y fija `Objection`. Verificar con tests: aprobar con diferencia 0 sin nota no lanza; aprobar con diferencia ≠ 0 sin nota lanza; objetar sin motivo lanza; aprobar/objetar desde `Pending` u `Objected`/`Approved` lanza.
- [x] 1.7 Crear `Interfaces/IPrefactureRepository.cs` (`IRepository<Prefacture>` más `GetByPersonAndPeriodAsync` y `GetByPeriodAsync`). Verificar: build.

## 2. Application — descuento por ausencias y contexto

- [x] 2.1 Crear `Billing/BillingDiscountCalculator.cs`: dado personId, período y costo mensual, usa `IAbsenceRepository.GetAllAsync` + `BusinessDayMath` para sumar los días hábiles dentro del período de las ausencias **aprobadas** de esa persona, y devuelve `AbsenceDiscount?` (`null` si no hay ausencias aprobadas que toquen el período; monto redondeado). Verificar con tests: sin ausencias aprobadas → `null`; con una ausencia `Requested`/`Rejected` que toca el período → `null` (no cuenta); con una `Approved` de 3 días hábiles sobre un mes de 20 → el monto esperado exacto.
- [x] 2.2 Crear `Billing/BillingContext.cs`: resuelve `externals()` — personas con `ProviderId != null`, con su célula actual (vía `IAllocationRepository`) y nombre de proveedor (vía `ICompanyRepository`) — y arma el DTO de una `Prefacture` combinando su snapshot congelado, el nombre de proveedor en vivo, el descuento (congelado si `Approved`, recalculado si no) y `Expected`/`Difference` calculados. Verificar con tests: `Expected` sin ausencias ni ajuste = costo mensual; con descuento y ajuste, la resta y suma exactas; `Difference` `null` sin prefacturado.

## 3. Application — DTOs y use cases

- [x] 3.1 DTOs del contrato en `DataTransferObjects/BillingDtos.cs`: `PrefactureDto`, `BillingAdjustmentDto`, `PrefactureDocumentDto`, `ImputationDto`, `ObjectionDto`, `AbsenceDiscountDto`, y los request DTOs (`GeneratePrefacturesRequest`, `RegisterPrefactureRequest`, `SetPrefacturedRequest`, `SetBillingStatusRequest`, `SetBillingAdjustmentRequest`). Verificar: un test que serializa a JSON y comprueba los nombres exactos del contrato (`personName`, `squadName`, `providerName`, `monthlyCost`, `absenceDiscount`, `expected`, `prefactured`, `difference`, `approvalNote`, `approvedAtUtc`).
- [x] 3.2 `GetPrefactures` (`GET /billing?period=`): 400 si el período falta o no es `YYYY-MM`; una fila por persona externa — existente o sintética en `None` sin persistir. Verificar con tests: período inválido lanza; persona externa sin registro aparece en `None`; persona externa con registro aparece con su estado real.
- [x] 3.3 `GetPrefactureById` (`GET /billing/{id}`): 404 si no existe. Verificar con test.
- [x] 3.4 `GeneratePrefactures` (`POST /billing/generate`): 400 si el período es inválido; idempotente, sólo crea para personas externas sin registro ese período, congelando su snapshot actual. Verificar con tests: primera vez crea todas; segunda vez sobre el mismo período no duplica ni toca las existentes.
- [x] 3.5 `RegisterPrefactureDocument` (`POST /billing/{id}/prefacture`) con su validador (número no vacío, fecha `YYYY-MM-DD`, monto > 0, moneda `COP`): 404 si no existe; 400 con datos inválidos o con las guardas del agregado (1.4). Verificar con tests: datos inválidos lanza 400 con el mensaje del mock; registrar sobre `Objected` reemplaza y vuelve a `Received`.
- [x] 3.6 `SetPrefacturedAmount` (`PUT /billing/{id}/prefactured`) y `SetBillingAdjustment`/`RemoveBillingAdjustment` (`PUT`/`DELETE /billing/{id}/adjustment`) con su validador: 404; 400 con las guardas del agregado (1.5) y con datos inválidos (monto de ajuste entero no cero, motivo del catálogo). Verificar con tests: cada 400 con su mensaje; el estado pasa a `InReview` desde `Received` y se mantiene en las demás.
- [x] 3.7 `SetBillingStatus` (`PUT /billing/{id}/status`) con su validador: 404; 400 fuera de `Received`/`InReview`, aprobar con diferencia ≠ 0 sin nota, u objetar sin motivo; aprobar recalcula el descuento vigente antes de congelarlo. Verificar con tests: aprobar con diferencia 0 sin nota funciona; aprobar con diferencia ≠ 0 con nota funciona y congela el descuento; objetar con motivo funciona; los tres 400.
- [x] 3.8 Registrar los seis use cases y sus validadores en `Application/DependencyInjection` bajo `// Prefacturación`. Verificar: build y el test de DI que resuelve el contenedor.

## 4. Infrastructure

- [x] 4.1 `PrefactureConfiguration` (tabla `Prefactures`; snapshot como columnas planas; `Adjustment`, `Document` (con `Imputation` anidada), `Objection`, `FrozenDiscount` como `OwnsOne`; período como string ≤7; índice único por `(PersonId, Period)`) y `PrefactureRepository`; `DbSet` en `ApplicationDbContext`, migración nueva y registro en DI. Verificar con un test sobre Sqlite en memoria: guardar una prefactura con documento e imputación completos, releerla y obtener los mismos valores; insertar dos con el mismo `(PersonId, Period)` viola el índice.
- [x] 4.2 Semillas: prefacturas del período actual y el anterior sobre personas externas ya sembradas con proveedor — una aprobada del período anterior con documento completo, una objetada del período anterior con motivo, una en revisión del período actual con documento sin orden de compra. Verificar: `dotnet run` y `GET /billing?period=<período actual>` y `<período anterior>` devuelven las filas esperadas con sus estados.

## 5. WebApi y contrato

- [x] 5.1 `BillingEndpoints`: las 8 rutas (`GET /billing`, `GET /billing/{id}`, `PUT`/`DELETE /billing/{id}/adjustment`, `POST /billing/{id}/prefacture`, `PUT /billing/{id}/prefactured`, `PUT /billing/{id}/status`, `POST /billing/generate`); `Produces` con 200/201, 400 y 404. Verificar: Swagger lista las 8 operaciones bajo `/api/v1/billing`.
- [x] 5.2 `Swagger/Examples/BillingExamples.cs` con una fila `None`, una `Pending`, una con documento e imputación incompleta, y una `Approved` con descuento congelado. Verificar: Swagger muestra los ejemplos.

## 6. Verificación

- [x] 6.1 `dotnet build` + `dotnet test` en verde (sólo las fallas pre-existentes de `RestClientBehaviorTests`).
- [x] 6.2 Smoke con Postgres local (`curl -sk`, `Idempotency-Key` en POST/PUT): `GET /billing?period=<actual>` trae las semillas y las filas `None` de quienes no tienen registro; `POST /billing/generate` crea las que faltan y repetirlo no duplica; `POST /billing/{id}/prefacture` con datos inválidos → 400, sobre la aprobada → 400; `PUT /billing/{id}/adjustment` mueve una `Received` a `InReview`; `PUT /billing/{id}/status` aprobar sin nota con diferencia → 400, con nota → 200 y descuento congelado; objetar sin motivo → 400, con motivo → 200; id inexistente → 404 en cada ruta.
- [x] 6.3 `backend/ENDPOINTS.md`: Prefacturación 8🟢, total del resumen actualizado, dejando anotado que el scope por chapter sigue pendiente (igual que los módulos anteriores).
