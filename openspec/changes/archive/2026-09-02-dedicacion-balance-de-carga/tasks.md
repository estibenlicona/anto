> **Nota de secuencia (anotada al aplicar).** Reescribir el contrato en 1.2 rompe
> a propósito todo lo que lo consume —adapter, mock, componentes, ficha y pruebas—
> hasta que los grupos 2 a 7 lo reconstruyen. `tsc` y la suite completa **no pueden
> estar en verde entre 1.2 y 6.8**; son puertas de fin de grupo, no de cada tarea.
> Donde una tarea individual dice "verificar con `tsc`", léase "sin errores nuevos
> fuera de los módulos que este change reescribe". La puerta real de árbol limpio
> es 7.5.

## 1. Renombre y contratos

- [x] 1.1 Renombrar en `features/dedication` el referente de "capacidad": endpoints `/dedication/capacities…` → `/dedication/collaborators…`, DTOs `CapacityDedication*` → `CollaboratorDedication*`, `CapacityDedicationContainer` → `CollaboratorDashboardContainer`, `CapacitiesTable` → `CollaboratorsTable`, `CapacityDedicationHeader` → `CollaboratorHeader`, métodos del servicio a `listCollaborators` / `getCollaborator` / `syncAll` / `syncCollaborator`, y los textos de interfaz ("las capacidades a cargo" → "los colaboradores a cargo"). Commit mecánico, antes de tocar lógica. Verificar con `tsc` sin errores, la suite existente en verde y `grep -ri "capacities\|CapacityDedication" src/` sin resultados.
- [x] 1.2 Reescribir `features/dedication/services/dedicationService.ts` con la forma nueva: `CapacityBreakdownDto`, `ReferenceDto`, `SprintExecutionDto` (con `snapshotStatus` y `sealedAt`), `UnplannedWorkDto`, `MultitaskingDto`, `BalanceEvidenceDto`, `BalanceSignalDto`, `SprintTrendPointDto`, `CollaboratorDedicationRowDto`, `CollaboratorDedicationSummaryDto`, `CollaboratorDedicationListDto` y `CollaboratorDedicationDetailDto`; `WorkItemDto` gana `epicId` y `addedAfterSprintStart`; quitar `pointsPerFtePerSprint`, `committedFte`, `assignedFte` como referencia, `deltaFte`, `reading` y `DEDICATION_READINGS`. Verificar con `tsc` y con `grep -r "reading\|committedFte\|pointsPerFte" src/features/dedication/services/` sin resultados.
- [x] 1.3 En `SprintConfig`, `useSprintConfig`, `sprint-config.handlers.ts` y `AdminSprintsPage`: quitar `pointsPerFtePerSprint`; agregar `sprintCloseTime` (`HH:mm`, "23:00"), `historyWindowSprints` (6; 3–12) y `minHistorySprints` (3; 2–6, nunca mayor que la ventana), con sus rótulos, la validación cruzada mínimo ≤ ventana en formulario y handler (`400`), y la tarjeta de qué usa el calendario reescrita. Verificar con las pruebas de `useSprintConfig`, `sprintConfigService` y `AdminSprintsPage` actualizadas en verde, incluida una que rechaza mínimo > ventana.

## 2. Reglas puras

- [x] 2.1 Crear `features/dedication/adapters/capacityFte.ts` con `computeAvailableFte({ contractualFte, businessDays, holidays, vacationDays, absenceDays, otherUnavailableDays })` → `{ availableFte, breakdown }`, reutilizando `countBusinessDays` de `features/absences/services/businessDays.ts`. Verificar con pruebas: 10 días con 1 festivo y 1 ausencia → 0.80; media jornada; contractual 0.5 sin descuentos → 0.50; `businessDays = 0` → 0; resultado acotado a `[0, contractualFte]`.
- [x] 2.2 Crear `features/dedication/adapters/history.ts` con `median(values)` (promedio de los dos centrales en longitud par) y `buildReference({ sprints, windowSprints, minSprints, squadSprints })` → `{ ownMedian, squadMedian, sealedSprintCount, sufficient, ownDeviation, squadDeviation }`, contando sólo sprints sellados. Verificar con pruebas: mediana sobre ventana par e impar; un sprint sin snapshot queda fuera y baja `sealedSprintCount`; histórico insuficiente marca `sufficient: false`; colaborador sin célula devuelve `squadMedian: null`.
- [x] 2.3 Crear `features/dedication/adapters/balanceSignal.ts` con las seis evidencias y sus umbrales como constantes nombradas (demanda vs histórico ±25 %, fuerte a 50 %; demanda por FTE disponible ±25 % / 50 %; cumplimiento −15 pp, fuerte a −30 pp, con `under` cuando es 100 % y la demanda está por debajo; carry-over +10 pp, fuerte a +25 pp; no planificado > 20 %, fuerte > 50 %; multitarea ≥ 3 iniciativas o WIP ≥ 4, fuerte a ≥ 5 / ≥ 6), la evaluación de cada una, el modificador de célula, la agregación por concurrencia y los motivos de no evaluable, más `SIGNAL_LABELS`, `SIGNAL_PHRASES` y `SIGNAL_VARIANTS`. Verificar con `tsc` y con las pruebas de 2.4.
- [x] 2.4 Probar `balanceSignal` con los escenarios de la spec: balanceado; una sola evidencia (carry-over alto) → Balanceado; una evidencia fuerte → Revisar; dos concordantes → Revisar; cuatro `over` → Posible sobreasignación; tres `under` → Posible subasignación; célula que acompaña baja un escalón; evidencias opuestas → techo en Revisar; evidencia no evaluada no cuenta pero se reporta; los tres motivos de no evaluable; y un colaborador sin célula que sí resulta evaluable. Verificar con la suite del módulo en verde.
- [x] 2.5 Borrar `features/dedication/adapters/dedicationReading.ts` y sus pruebas, y toda referencia a `READING_LABELS`, `READING_VARIANTS`, `isComparative`, `isOutOfLine` y `READING_TOLERANCE_FTE`. Verificar con `grep -ri "dedicationReading\|READING_" src/` sin resultados y `tsc` limpio.

## 3. Mock

- [x] 3.1 Resembrar `mocks/handlers/dedication.seeds.ts`: por usuario de DevOps, siete sprints con fechas alrededor de `TODAY` y su procedencia de snapshot, festivos y otras indisponibilidades por sprint, historias con `epicId`, `epicTitle`, puntos, estado, tablero, `addedAfterSprintStart` y URL, la serie de estados para derivar el WIP, y actividad por día. Verificar que las semillas cubren los casos de la spec (balanceado, revisar, sobreasignación, subasignación con célula que acompaña, sin identidad, sin sprints, histórico insuficiente, sin célula, tiempo parcial, sprint con festivo y ausencia, sprint sin snapshot, sprint provisional, historias tardías, épicas mapeadas y sin mapear, sprint sin actividad) con una prueba que las enumera.
- [x] 3.2 Reescribir `mocks/handlers/dedication.handlers.ts` con `GET /dedication/collaborators`, `GET /dedication/collaborators/:personId`, `POST /dedication/collaborators/sync` y `POST /dedication/collaborators/:personId/sync` (600 ms, `404`, `409`), derivando de los mocks de personas, detalle de persona, asignaciones, células, iniciativas, ausencias y configuración de sprints, calculando con los módulos del paso 2, sin reescribir snapshots sellados al sincronizar, exponiendo `getBalanceSignalsSnapshot()` y con `reset`. Verificar con las pruebas de 3.4.
- [x] 3.3 En `personDetailService` y `personDetail.handlers.ts`: `devOpsIdentity.currentSprint` pasa a traer sprint, SP comprometidos, mediana histórica, FTE disponible sobre contractual, señal y conteo de evidencias, o el motivo de no evaluable; quitar `committedFte`, `assignedFte`, `deltaFte` y `reading` de DTO, adapter, semillas y fixtures. Verificar con las pruebas de `personDetail` actualizadas en verde y `tsc` limpio.
- [x] 3.4 Probar el handler: listado con resumen por señal y orden por señal accionable; filtros `squadId` / `signal` / `search` y paginación; detalle con y sin `sprint`; la señal sigue a una ausencia aprobada, a un cambio de asignación y a un cambio de ventana de histórico; un sprint sin snapshot no entra al histórico; `sync` recalcula lo provisional y deja lo sellado idéntico; `sync` responde `200` / `404` / `409`; vincular una identidad deja al colaborador en "No evaluable · histórico insuficiente"; el detalle de persona trae la señal. Verificar con la suite de handlers en verde.

## 4. Componentes compartidos

- [x] 4.1 Crear `components/CapacityFteBar.tsx` (pista = FTE contractual, relleno = FTE disponible, muescas por concepto descontado, cifras "0.80 / 1.0 FTE", `Tooltip` con el desglose; variantes `row`, `card`, `compact`). Verificar con pruebas de render de cifras, desglose accesible y variante sin descuentos.
- [x] 4.2 Crear `components/DemandBar.tsx` (marca = mediana propia, marca secundaria atenuada = mediana de célula, relleno = SP del sprint, cifras "28 SP · habitual 22"; sin marca de célula sin célula; sólo la cifra con histórico insuficiente). Verificar con pruebas de los tres casos.
- [x] 4.3 Crear `components/BalanceSignalIcon.tsx` (20 px, columna de 72 px, `aria-label` y `Tooltip` con señal y conteo de evidencias, forma distinta además de color, sin texto ni enlace) y `components/BalanceSignalBadge.tsx` (marca de estado con texto y frase). Verificar con pruebas de los cinco valores de señal y de que el icono no renderiza texto visible.
- [x] 4.4 Crear `components/EvidenceList.tsx` (una fila por evidencia con nombre, dirección, cifra, umbral y estado —contó / neutra / no evaluada—, más la fila del modificador de célula). Verificar con pruebas que incluyen evidencias no evaluadas y el modificador aplicado.
- [x] 4.5 Reescribir `features/dedication/adapters/DedicationAdapter.ts` para el vocabulario nuevo (FTE con dos decimales, SP entero, porcentajes con un decimal, fechas del sprint, "Actualizado hace 12 min", la línea de resumen en lenguaje natural, rótulos de procedencia del snapshot, desglose de capacidad, filas del calendario de actividad) y borrar `DedicationBar` y `ReadingIcon`. Verificar con las pruebas del adapter en verde y `grep -r "DedicationBar\|ReadingIcon" src/` sin resultados.

## 5. Listado

- [x] 5.1 Reescribir `useCollaboratorDedication` (filtros con `signal`, búsqueda con debounce, paginación, `refetch`) conservando `useDedicationSync`. Verificar con pruebas del hook contra el mock: filtro por señal, debounce y cambio de página.
- [x] 5.2 Reescribir `DedicationStatsCards` → `BalanceStatsCards` con las cuatro tarjetas por señal (sobreasignación, subasignación, revisar, no evaluables con desglose por motivo), nombrando colaboradores cuando son pocos. Verificar con pruebas de conteos y del desglose de motivos.
- [x] 5.3 Reescribir `CollaboratorsTable` con las columnas Colaborador · Célula e iniciativa · Sprint (con procedencia) · Capacidad · Demanda · Multitarea · Balance, toolbar con búsqueda y filtros de Célula y Señal, orden por señal accionable, esqueleto, vacío, error y `PaginationBar`. Verificar con pruebas de que no existen columnas de FTE comprometido, FTE asignado, porcentajes ni actividad.
- [x] 5.4 Actualizar `DedicationContainer` con los cuatro indicadores, la tabla, la acción de actualizar y la hora en la franja del breadcrumb, toast y error. Verificar con una prueba de contenedor que renderiza indicadores + tabla y dispara la actualización.
- [x] 5.5 Probar el listado extremo a extremo contra el mock: orden por señal accionable, una fila leída completa, los cinco iconos de señal, filtro por señal y por célula, fila no evaluable con su motivo, y actualizar todos refrescando la hora. Verificar con la suite del feature en verde.

## 6. Dashboard del colaborador

- [x] 6.1 Reescribir `CollaboratorDashboardContainer` en `/app/lead/dedicacion/:personId` con `useCollaboratorDedicationDetail(personId, sprint)` (sprint en la URL), breadcrumb y los estados sin identidad, sin sprints, histórico insuficiente, cargando y error. Verificar con pruebas de los cinco estados.
- [x] 6.2 Actualizar `CollaboratorHeader` (vuelta al listado, avatar, nombre, cargo, célula con iniciativa y talla, FTE declarado rotulado como dato que la célula reporta, acciones Actualizar / Ver ficha / Reasignar reutilizando `useReassignPerson` y `ReassignPersonDrawer`) y `SprintTabs` (cada sprint con su procedencia). Verificar con una prueba de que reasignar refresca el encabezado sin cambiar la señal.
- [x] 6.3 Crear `BalanceHeaderCards` con las cuatro tarjetas Capacidad / Demanda / Referencia / Balance y la línea de resumen en lenguaje natural debajo. Verificar con una prueba del caso de la spec: "0.80 / 1.0 FTE", "28 SP", "22 SP", la señal y la línea "28 SP comprometidos vs 22 SP habituales · 3 iniciativas activas · 18 % carry-over histórico".
- [x] 6.4 Crear `WhyThisSignalPanel` (con `EvidenceList` y el modificador de célula) y `ReferencePanel` (las tres cifras alineadas con sus desviaciones; dos filas sin célula). Verificar con pruebas de los casos "colaborador bajo en célula normal" y "colaborador bajo en célula baja".
- [x] 6.5 Crear `SprintExecutionPanel` (con procedencia y fecha del sellado, y "Sin snapshot" en advertencia cuando falta), `UnplannedWorkPanel` (comprometido al inicio, agregado, total y proporción) y `MultitaskingPanel` (iniciativas con nombre o épica y sus SP, HUs comprometidas y WIP). Verificar con pruebas del caso 22 + 8 = 30 SP y del sprint sin snapshot.
- [x] 6.6 Crear `TrendPanel` con una fila por sprint de la ventana (SP comprometidos y completados, % de cumplimiento, carry-over %), del más antiguo al más reciente, sprints sin snapshot marcados y excluidos, y una línea de cumplimiento. Verificar con una prueba de la serie 94 / 91 / 88 / 79 / 76 % y de un punto faltante.
- [x] 6.7 Actualizar `SprintStoriesPanel` con la marca de si la historia entró después del inicio del sprint y la épica junto a la iniciativa mapeada, conservando `ActivityCalendarPanel` sin cambios. Verificar con pruebas de historia tardía, épica sin mapear y mapa de actividad con y sin actividad.
- [x] 6.8 Probar el dashboard extremo a extremo: abrir con el sprint en curso, cambiar de sprint por pestaña, por qué esta señal con evidencias neutras y no evaluadas, sprint sin snapshot, y los estados sin identidad / sin sprints / histórico insuficiente. Verificar con la suite del contenedor en verde.

## 7. Integración y limpieza

- [x] 7.1 Cambiar el indicador *Dedicación real* de la ficha a `BalanceSignalBadge` con su frase, "28 SP · habitual 22", la capacidad del sprint y "Ver dedicación"; "No evaluable" con su motivo; sin identidad como hoy; y rotular *Asignado* como lo que declara la célula. Verificar con las pruebas de `PersonDetailStatsCards`, adapter y fixtures actualizadas en verde.
- [x] 7.2 Cambiar `useDedicationAttentionCount` para contar sólo señales de posible sobreasignación y subasignación, y actualizar `features/chapter-lead-shell/navigation.ts`. Verificar con pruebas de layout y navegación: el badge cuenta las dos señales fuertes y desaparece cuando todos leen Balanceado, Revisar o No evaluable.
- [x] 7.3 Actualizar `context/docs/Roles_y_Permisos_Plataforma.md` para describir Dedicación real con el modelo nuevo (capacidad, demanda, referencia y señal de balance) en lugar de la lectura FTE comprometido vs asignado. Verificar leyendo el doc: no queda ninguna mención a FTE comprometido ni a las lecturas Por encima / Por debajo / En línea.
- [x] 7.4 Documentar en la cabecera de `dedicationService.ts` qué debe implementar el backend real: el job que sella el snapshot al cierre según `sprintCloseTime`, el origen de festivos y otras indisponibilidades, y el historial de estados de work item del que sale el WIP. Verificar que el comentario existe y nombra los tres puntos.
- [x] 7.5 Correr `tsc`, lint, prettier y la suite completa, y verificar en `pnpm dev:mock` el listado con las cinco señales, el dashboard de un colaborador con sobreasignación, el de uno cuya célula acompaña (la señal baja un escalón), el de uno con histórico insuficiente, un sprint sin snapshot, la redirección de `/app/lead/backlog` y el Calendario de sprints con sus cinco campos y la validación cruzada.
