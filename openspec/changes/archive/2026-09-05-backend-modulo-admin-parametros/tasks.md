# Tareas — Backend: módulo Admin (parámetros del modelo)

## 1. Domain

- [x] 1.1 Crear `ValueObjects/QuestionDimension.cs` (VO cerrado estilo `PersonRole`: las 7 dimensiones en el orden de referencia, `ValidValues`, `From(string)` con coincidencia exacta y `DomainException` listando las válidas). Verificar con `QuestionDimensionTests` (las 7 en orden, `From` inválido lanza).
- [x] 1.2 Crear `Interfaces/ISingleDocumentRepository<T>` (`GetAsync`, `AddAsync`, `Update`; `T : AggregateRoot`). Verificar: build.
- [x] 1.3 Crear `Entities/SprintConfiguration.cs` (`AggregateRoot`; ctor + `Update(weeks, sprintsPerQuarter, hoursPerSprint, sprintCloseTime, historyWindowSprints, minHistorySprints)` con rangos 1–4 / 4–8 / 20–400 / `HH:mm` / 3–12 / 2–6 y `min ≤ ventana`, lanzando `DomainException`). Verificar con `SprintConfigurationTests` (cada límite, min > ventana, hora inválida).
- [x] 1.4 Crear `Entities/TallaBandSet.cs` + `Entities/TallaBand.cs` (poseída: `Position`, `Talla` ≤10, `PmMin`, `PmMax`, `Lectura` ≤200) con `Replace(boundaries, bands)`: exactamente 4 cortes crecientes con ≥5 puntos entre sí y contra 0/100, exactamente 5 bandas, talla no vacía y única, `pmMin ≥ 0`, `pmMin ≤ pmMax`, lectura no vacía; conserva el orden. Verificar con `TallaBandSetTests` (cortes desordenados/pegados, 4 bandas, talla repetida, pmMin > pmMax).
- [x] 1.5 Crear `Entities/CapabilityMix.cs` + `Entities/CapabilityMixRow.cs` (poseída: `Position`, `Key` ≤50 = `id` del contrato, `Capacidad` ≤100, `PorTalla` diccionario talla→int) con `Replace(rows)`: key no vacía y única, nombre no vacío y único sin distinguir mayúsculas ni espacios en los bordes, cantidades ≥ 0; conserva el orden. Verificar con `CapabilityMixTests` (key repetida, «QA Engineer» vs « qa engineer », cantidad negativa, lista vacía válida).
- [x] 1.6 Crear `Entities/QuestionPool.cs` + `Entities/PoolQuestion.cs` (poseída: `Position`, `Code` ≤50 = `id` del contrato, `Dimension` (VO), `Texto` ≤500, `Peso` int) con `Replace(questions)`: código no vacío y único, texto no vacío, peso ≥ 1; conserva el orden. Verificar con `QuestionPoolTests` (código repetido, dimensión inválida, peso 0, texto en blanco).

## 2. Application

- [x] 2.1 Crear `ModelParameters/ModelParameterDefaults.cs` con los cuatro agregados de referencia (calendario 2/6/80/23:00/6/3; cortes 20/40/60/80 y las 5 bandas con pm y lectura del mock; Backend Dev / QA Engineer / Arquitecto con sus cantidades; las 30 preguntas del modelo v7 con id, dimensión, texto y peso — copiar de `question-pool.handlers.ts`). Verificar con `ModelParameterDefaultsTests`: 30 preguntas, 4/4/4/5/5/4/4 por dimensión, peso total 70, 5 bandas, 3 capacidades, y que cada default pasa las invariantes de su agregado.
- [x] 2.2 DTOs del contrato en `DataTransferObjects/ModelParameterDtos.cs`: `SprintConfigDto`, `TallaBandsDto` + `TallaBandDto`, `CapabilityMixRowDto` (`Id`, `Capacidad`, `PorTalla`), `QuestionPoolRowDto` (`Id`, `Dimension`, `Texto`, `Peso`); `ModelParameterMappings` agregado→DTO conservando el orden por `Position`. Verificar: build y un test de mapping que serializa a JSON y comprueba los nombres `capacidad`, `porTalla`, `texto`, `peso`, `talla`, `pmMin`, `pmMax`, `lectura`, `boundaries`, `bands`.
- [x] 2.3 Requests + validadores: `SaveSprintConfigRequest` (6 campos, se liga del cuerpo), `SaveTallaBandsRequest(Boundaries, Bands)` (se liga del cuerpo), `SaveCapabilityMixRequest(Rows)`, `SaveQuestionPoolRequest(Questions)`; un `AbstractValidator` por request con los mensajes en español de design.md (un mensaje por regla; el de duplicados nombra el valor repetido). Verificar con `SaveSprintConfigValidatorTests`, `SaveTallaBandsValidatorTests`, `SaveCapabilityMixValidatorTests`, `SaveQuestionPoolValidatorTests` (cada regla → su mensaje; cuerpo válido → sin errores).
- [x] 2.4 Use cases de lectura `GetSprintConfig`, `GetTallaBands`, `GetCapabilityMix`, `GetQuestionPool` (`repo.GetAsync() ?? ModelParameterDefaults.X()` → DTO) y de guardado `SaveSprintConfig`, `SaveTallaBands`, `SaveCapabilityMix`, `SaveQuestionPool` (validar → obtener o crear → `Replace`/`Update` → `AddAsync` si es nuevo o `Update` → `SaveChangesAsync` → DTO). Registrar los 8 en `Application/DependencyInjection` bajo `// Admin (parámetros del modelo)`. Verificar con `ModelParameterUseCaseTests` (Moq): sin fila responde el default sin llamar `AddAsync`; primer guardado llama `AddAsync` + `SaveChangesAsync`; segundo guardado reemplaza y conserva el orden enviado; request inválido lanza `ValidationException` sin tocar el repositorio.

## 3. Infrastructure

- [x] 3.1 `Repositories/SingleDocumentRepository<T>` (`FirstOrDefaultAsync` con tracking para poder actualizar; `AddAsync`; `Update`) y registro en `Infrastructure/DependencyInjection` para `SprintConfiguration`, `TallaBandSet`, `CapabilityMix` y `QuestionPool`. Verificar: `DependencyInjectionTests` resuelve los cuatro `ISingleDocumentRepository<T>`.
- [x] 3.2 Configuraciones EF: `SprintConfigurationConfiguration` (tabla `SprintConfigurations`, `SprintCloseTime` ≤5), `TallaBandSetConfiguration` (`Boundaries` como JSON string con converter + `ValueComparer`; `OwnsMany` `TallaBands` con clave sombra y `Position`), `CapabilityMixConfiguration` (`OwnsMany` `CapabilityMixRows`; `PorTalla` como JSON string con converter + comparer), `QuestionPoolConfiguration` (`OwnsMany` `PoolQuestions`; `Dimension` con converter a string ≤50). `DbSet`s en `ApplicationDbContext`. Verificar con un test de repositorio sobre Sqlite en memoria (como `PersonRepositoryTests`): guardar cada agregado, releer y obtener los mismos cortes/cantidades en el mismo orden; `EnsureCreated` no falla.

## 4. WebApi

- [x] 4.1 `Endpoints/AdminEndpoints.cs`: grupo `api/v{version:apiVersion}/admin` (tag `Admin`) con `GET/PUT sprint-config`, `GET/PUT talla-bands`, `GET/PUT capability-mix` (cuerpo `CapabilityMixRowDto[]` envuelto en `SaveCapabilityMixRequest`), `GET/PUT question-pool` (cuerpo `QuestionPoolRowDto[]` envuelto en `SaveQuestionPoolRequest`); `Produces` 200 y 400. Verificar: `dotnet run` + Swagger lista las 8 operaciones bajo `/api/v1/admin/...`.
- [x] 4.2 `Swagger/Examples/AdminExamples.cs` con ejemplos de las cuatro formas (los defaults). Verificar: Swagger muestra los ejemplos en request y response.

## 5. Verificación

- [x] 5.1 `dotnet build` + `dotnet test` en verde (solo las 9 fallas pre-existentes de `RestClientBehaviorTests`).
- [x] 5.2 Smoke con InMemory (`curl -sk`, `Idempotency-Key` en los `PUT`): los 4 `GET` responden los defaults; `PUT sprint-config` con `minHistorySprints` 7 → 400 con el mensaje; `PUT sprint-config` válido (ventana 10) → 200 y el `GET` siguiente lo devuelve; `PUT talla-bands` con cortes 20/24/60/80 → 400 (ancho mínimo; 20/25/60/80 es válido porque el mínimo de 5 es inclusivo, como en el mock); `PUT talla-bands` válido reordenando lecturas → el `GET` conserva el orden; `PUT capability-mix` con «QA Engineer» y «qa engineer» → 400; `PUT capability-mix` con una capacidad nueva → 200 en el orden enviado; `PUT question-pool` con dimensión inventada y con peso 0 → 400; `PUT question-pool` quitando una pregunta → 200 con 29.
- [x] 5.3 `node backend/tools/contract-coverage.mjs` sigue 88/88; `backend/ENDPOINTS.md`: Admin 8🟢 con la nota «defaults del mock hasta el primer PUT», total 26→34 y nota en Iniciativas de que `evaluation-model` se compone desde Admin.
