## 1. Domain

- [x] 1.1 Crear `ValueObjects/PlanActionStatus.cs` (catálogo cerrado `InProgress`/`Done`). Verificar con test: valores en el orden del contrato.
- [x] 1.2 Crear `Entities/PlanAction.cs` (`AggregateRoot`): ctor `(Guid personId, Guid skillId, int fromLevel, int targetLevel, string dueMonth, string title)` — persona y habilidad obligatorias, `fromLevel`/`targetLevel` en 1-4, `targetLevel > fromLevel`, `dueMonth` con forma `AAAA-MM`, título obligatorio; nace `InProgress`. `SetStatus(PlanActionStatus)`. Verificar con tests: nivel objetivo igual o menor al de partida lanza, mes con forma inválida lanza, título vacío lanza, nace `InProgress`, `SetStatus` cambia el estado sin tocar ningún otro campo.
- [x] 1.3 Crear `Interfaces/IPlanActionRepository.cs` (`IRepository<PlanAction>` más `GetByPersonAsync(personId)` y `GetAllAsync` ya heredado para el pendiente de gestión del chapter). Verificar: build.

## 2. Application — cálculo compartido

- [x] 2.1 Crear `CareerPlan/CareerPlanContext.cs`: arma, para todas las personas del chapter, la evaluación cerrada más reciente de cada una (por `Cycle` más alto, comparación de texto) y expone `BuildSpanAsync()` (`SpanMatrixDto` sobre las habilidades `Active`, con `expectedLevel`/`gap` contra el cargo vigente de cada persona) y `BuildPlanAsync(person)` (`PersonPlanDto` sobre las habilidades que esa evaluación calificó, usando `FrozenSkillName`/`FrozenGroup`/`FrozenLevels` para nombre, grupo y criterios, y el catálogo vigente sólo para `expectedLevel`). Verificar con tests: persona sin evaluación cerrada → celda de la matriz con `level: null` y fila `evaluated: false`, perfil con `skills: []` y `cycle: null`; persona con evaluación cerrada y catálogo que subió una exigencia después → `expectedLevel` de hoy, no el congelado; habilidad calificada y luego desactivada → sigue en el perfil pero no en la matriz.
- [x] 2.2 Crear `CareerPlan/SpanSummaryCalculator.cs`: puerto literal de `buildSpanSummary`/`gapsInCycle` — `totalGaps`/`criticalGaps` (`gap >= 2`) sobre personas evaluadas; `peopleAtRisk` (`gapCount >= 3`, orden por cantidad desc. y nombre); serie `trend` (un punto por ciclo cerrado distinto, comparando siempre contra la exigencia de hoy, nunca la de cada ciclo) y `previousCycle` (penúltimo punto o `null`); `topSkills` (peso = suma de niveles que faltan, no cuenta de personas; top 4, descarta peso 0, empate por nombre). Verificar con tests: los mismos escenarios del spec (dos brechas de un nivel y una de tres — la de tres pesa más que cuenta simple no daría; ciclo con catálogo que subió una exigencia no "mejora" la serie retroactivamente; sin ciclo anterior, `previousCycle` es `null`).
- [x] 2.3 Crear `CareerPlan/CareerPlanPendingCalculator.cs`: `unassessed` (personas sin evaluación cerrada), `overduePlans` (acciones `InProgress` con `dueMonth` anterior al mes actual, comparación de texto en hora local vía `TimeProvider`), `positionsWithoutLevel` (cargos distintos del chapter con alguna habilidad activa sin nivel declarado para ese cargo), `gapsWithoutPlan` (pares persona-habilidad en brecha sin ninguna acción `InProgress` sobre esa habilidad para esa persona — una acción `Done` no cuenta). Verificar con tests: acción `Done` sobre la única brecha de una habilidad la deja otra vez sin plan; un pendiente en cero se calcula igual, no se omite.

## 3. Application — DTOs y use cases

- [x] 3.1 DTOs del contrato en `DataTransferObjects/CareerPlanDtos.cs`: `SpanMatrixDto`/`SpanSkillDto`/`SpanPersonDto`/`SpanCellDto`, `SpanSummaryDto`/`SpanPersonRefDto`/`SpanCyclePointDto`/`SpanFocusSkillDto`/`SpanPendingDto`, `PersonPlanDto`/`PlanSkillDto`/`PlanActionDto`. Verificar: un test que serializa a JSON y comprueba los nombres exactos del contrato (`personName`, `assessmentClosedAtUtc`, `expectedLevel`, `metCriteria`, `levelTotal`, `missingCriteria`, `expectedTotal`, `gapCount`, `totalGaps`, `peopleWithGap`, `positionsWithoutLevel`, `gapsWithoutPlan`, `fromLevel`, `targetLevel`, `dueMonth`).
- [x] 3.2 `GetSpanMatrixUseCase` (`GET /career-plan/span`) y `GetSpanSummaryUseCase` (`GET /career-plan/span/summary`), ambos sobre `CareerPlanContext`/`SpanSummaryCalculator`/`CareerPlanPendingCalculator`. Verificar con tests: sin ninguna persona evaluada, matriz con `evaluated: false` en todas las filas y resumen con los cuatro indicadores en cero salvo `totalPeople`.
- [x] 3.3 `GetPersonPlanUseCase` (`GET /career-plan/people/{personId}/plan`): 404 si la persona no existe. Verificar con tests: persona sin evaluación cerrada responde plan con `skills: []`, `cycle: null` y sus acciones (si tuviera, aunque no debería poder tener sin brecha).
- [x] 3.4 `CreatePlanActionUseCase` (`POST .../plan/actions`): resuelve el plan vigente de la persona (404 si no existe), valida habilidad evaluada con brecha registrada (400 con el nombre de la habilidad si no hay brecha), `targetLevel` 1-4 y mayor al alcanzado (400), `dueMonth` con forma `AAAA-MM` (400), `title` no vacío (400); `FromLevel` congelado al nivel alcanzado, nace `InProgress`, responde 201 con el plan completo. Verificar con tests: cada 400 del contrato, y que dos acciones sobre habilidades distintas de la misma persona conviven.
- [x] 3.5 `SetPlanActionStatusUseCase` (`PUT .../plan/actions/{actionId}/status`): 404 si la acción no existe para esa persona; cambia sólo el estado, nunca la brecha (que sigue derivándose de la evaluación, no de la acción). Verificar con test: marcar `Done` no cambia el `gap` que `GetPersonPlanUseCase` reporta para esa habilidad.
- [x] 3.6 Registrar los cinco use cases y los dos calculadores en `Application/DependencyInjection` bajo `// Competencias (span y planes)`. Verificar: build.

## 4. Infrastructure

- [x] 4.1 `PlanActionConfiguration` (tabla `PlanActions`; `Status` con converter a string; índice por `PersonId`) y `PlanActionRepository`. `DbSet` en `ApplicationDbContext`, migración nueva y registro en DI. Verificar con un test sobre Sqlite en memoria: guardar una acción y releerla con el mismo estado y niveles.
- [x] 4.2 Semillas: una o dos acciones de plan sobre las evaluaciones cerradas que Evaluaciones ya sembró (una brecha real de María en SQL, otra de Laura) — al menos una `InProgress` con `dueMonth` vencido (para el pendiente) y, si aplica, una `Done` sobre una brecha que así vuelve a quedar sin plan. Verificar: `dotnet run` y `GET /career-plan/span/summary` devuelve `criticalGaps`/`peopleAtRisk`/`pending` con cifras no todas en cero.

## 5. WebApi

- [x] 5.1 `CareerPlanEndpoints`: las 5 rutas (`GET /career-plan/span`, `GET /career-plan/span/summary`, `GET /career-plan/people/{personId}/plan`, `POST .../plan/actions`, `PUT .../plan/actions/{actionId}/status`) con sus `Produces` (200/201, 400, 404 según el contrato). Verificar: Swagger lista las 5 operaciones bajo `/api/v1/career-plan`.
- [x] 5.2 `Swagger/Examples/CareerPlanExamples.cs` con una matriz de dos personas y dos habilidades (una brecha crítica, una al nivel), un resumen con las cuatro cifras y un plan con una acción en curso. Verificar: Swagger muestra los ejemplos.

## 6. Verificación

- [x] 6.1 `dotnet build` + `dotnet test` en verde (sólo las fallas pre-existentes de `RestClientBehaviorTests`).
- [x] 6.2 Smoke con Postgres local (`curl -sk`): `GET /career-plan/span` trae una fila por persona y columna por habilidad activa; `GET /career-plan/span/summary` trae brechas críticas, cobertura, personas en riesgo y pendientes coherentes con las semillas; `GET /career-plan/people/{id}/plan` de la persona con brecha sembrada trae la habilidad con `gap > 0`; `POST .../plan/actions` con una habilidad sin brecha → 400; con datos válidos → 201 y la acción aparece en el plan; `PUT .../status` a `Done` → 200 y el `gap` de esa habilidad no cambia; persona inexistente en cualquiera de las tres rutas de persona → 404.
- [x] 6.3 `backend/ENDPOINTS.md`: las cinco filas de Competencias a 🟢, total del resumen actualizado.
