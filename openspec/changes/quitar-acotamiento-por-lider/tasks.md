## 1. Mocks: quitar el acotamiento

- [x] 1.1 `frontend/src/mocks/handlers/chapters.ts`: retirar `holderChapterId(request)` y la lectura del `oid` del `Authorization` que la alimenta. Conservar `CHAPTERS`, `leadEntraObjectIdOf` y todo lo que la ficha de persona usa para mostrar chapter y su lead: lo que se quita es el papel de regla de alcance, no el dato. Verificar: `grep -rn "holderChapterId" frontend/src` no devuelve nada fuera de este archivo antes de borrarlo, y nada después.
- [x] 1.2 `frontend/src/mocks/handlers/people.handlers.ts`: `peopleFor(request)` deja de filtrar — se elimina y sus llamadores pasan al snapshot completo de personas. Alcanza al resumen agregado, al listado paginado y al catálogo de líderes técnicos. Verificar: los tres endpoints devuelven las 18 personas sembradas con cualquier token, y también sin token.
- [x] 1.3 `frontend/src/mocks/handlers/people.handlers.ts`, alta de persona: la persona nueva nace con `chapterId: null` en vez de heredar el chapter del titular (design.md, Decisión 4). Verificar con test: un `POST` de alta devuelve la persona con `chapterId` en `null`, y esa persona aparece en el listado.
- [x] 1.4 `frontend/src/mocks/handlers/scope.ts`: eliminar el archivo. Trasladar como comentario, al módulo que quede cruzando personas con asignaciones, la advertencia de filtrar las dos puntas — vuelve a hacer falta cuando exista la vista personal. Verificar: `grep -rn "vistaDe\|scope\"" frontend/src/mocks` sin resultados; `pnpm typecheck` limpio.
- [x] 1.5 Handlers consumidores de `vistaDe`: `chapter.handlers.ts` (resumen de capacidad), `squads.handlers.ts` (equipo y cifras por célula, y el FTE del resumen), `absences.handlers.ts` (calendario del mes y cola por aprobar), `dedication.handlers.ts` (listado, detalle y sync). Cada uno pasa a leer los snapshots completos de personas y asignaciones. Verificar: la Torre de control suma el FTE de las 18 personas; el resumen de Células reporta el FTE total; el mes de Ausencias lista las de cualquier persona; el balance de carga trae una fila por cada colaborador.
- [x] 1.6 Handlers consumidores de `peopleFor`: `allocations.handlers.ts` (asignaciones por célula), `billing.handlers.ts` (prefacturas por externo), `career-plan.handlers.ts` (matriz del span, serie histórica de brechas, cargos sin nivel). Verificar: cada célula muestra todo su equipo; la facturación lista a todos los externos; la matriz tiene una fila por cada persona con evaluación cerrada.
- [x] 1.7 `frontend/src/mocks/handlers/personDetail.handlers.ts`: la cobertura de stacks y los compañeros dejan de acotarse por el chapter **de la persona mirada** (hoy acota por ése, no por el de quien mira). Verificar con test: "cuántas personas más cubren este stack" cuenta sobre todas las personas registradas.

## 2. Mocks: pruebas del acotamiento

- [x] 2.1 `frontend/src/mocks/handlers/__test__/chapters.scope.test.ts`: eliminar el archivo entero (14 pruebas dedicadas al acotamiento: "devuelve a cada lead sólo su gente", "acota las asignaciones junto con las personas", "acota la serie histórica de brechas", "acota la facturación", etc.). No se reescriben acá: lo que afirman vuelve, con otro sujeto, cuando exista la vista personal. Verificar: `pnpm test` no reporta el archivo y no queda ninguna prueba huérfana importándolo.
- [x] 2.2 Revisar las suites que montan sesión y podían depender del conjunto acotado: `dedication.handler.test.ts`, `personDetail.handler.test.ts`, `absences.handler.test.ts` y `squads.handler.test.ts`. Ajustar las cifras esperadas que cambian de denominador; conservar las que no dependen del alcance. Verificar: cada archivo pasa aislado antes de correr la suite completa.

## 3. Textos visibles: quitar "chapter"

- [x] 3.1 Células y Equipos: `features/squads/components/SquadsStatsCards.tsx:87` (`"41% del chapter"` → `"41%"`), `features/squads/components/SquadFormDrawer.tsx:90` ("Registra una nueva célula del chapter." → "Registra una nueva célula."), `features/teams/components/TeamFormDrawer.tsx:71` (ídem con equipo). Verificar: la card de Células lee "7.0 de 17.0 FTE · 41%" y ninguna frase queda coja al perder el complemento.
- [x] 3.2 Torre de control: `features/control-tower/components/ControlTowerStatsCards.tsx:28` (`title="FTE DEL CHAPTER"` → `"FTE"`) y `:45` ("del FTE del chapter sin asignar" → "del FTE sin asignar"); `features/control-tower/ControlTowerContainer.tsx:26` (la descripción de la pantalla). Verificar con tests: los de la Torre que fijan esos textos, ajustados y en verde.
- [x] 3.3 Ausencias: `features/absences/AbsencesContainer.tsx:139` (estado vacío del mes) y `features/absences/components/AbsencesStatsCards.tsx:76` (`"de N FTE del chapter"` → `"de N FTE"`). Verificar: el estado vacío y la lectura de impacto se leen enteros, sin el complemento.
- [x] 3.4 Competencias: `features/career-plan/components/SpanFocusSkills.tsx:34` ("Agregado del chapter, no de una persona" → "Agregado del conjunto, no de una persona"), `features/career-plan/components/SpanSummaryCards.tsx:112`, `features/career-plan/SpanMatrixContainer.tsx:115` (mensaje de error del resumen) y `:139` (estado vacío de la matriz). Verificar con tests: `SpanMatrixContainer.test.tsx` ajustado y en verde.
- [x] 3.5 Dedicación: `features/dedication/DedicationContainer.tsx:91` (estado vacío: "Cuando tu chapter tenga personas…" → "Cuando haya personas…"). Revisar además `features/dedication/components/CollaboratorsTable.tsx:230`, que renderiza "{total} de {chapterTotal} personas": el identificador no se ve, pero la frase ahora cuenta sobre el total registrado. Verificar: `DedicationContainer.test.tsx` ajustado y en verde.
- [x] 3.6 Personas y Líneas: `features/people/components/PeopleList.tsx:103` (estado vacío inicial), `features/people/components/detail/EditStacksDrawer.tsx:124` (`label="Del catálogo del chapter"` → `"Del catálogo"`) y `:199` (`title="El chapter quedaría sin cobertura"` → "Nadie quedaría cubriendo este stack"), `features/people/components/detail/PersonProfilePanel.tsx:105` ("· lidera este chapter"), `features/people/components/detail/PersonStacksPanel.tsx:58`, `features/expertise-lines/components/ExpertiseLinesHeader.tsx:27`. Verificar: `grep -rn` de "chapter" en cadenas visibles de `frontend/src/features` no devuelve nada.

## 4. Regla de idioma verificada

- [x] 4.1 `frontend/src/shared/__test__/uiWriting.test.ts`: sumar "chapter" a las formas que el verificador rechaza en texto visible, sin que identificadores como `chapterId`, `chapterFte`, `chapterTotal` o las rutas `/chapter/...` lo hagan fallar. Verificar: el verificador pasa sobre el código ya corregido, y falla si se reintroduce a mano un texto visible con la palabra.

## 5. Verificación final

- [x] 5.1 Suites completas del front: `pnpm test`, `pnpm lint`, `pnpm typecheck` en verde. Verificar: los tres comandos en 0.
- [x] 5.2 Verificación visual sobre mocks con los tres perfiles (Ana administradora, Tomás líder de expertise, Lucía líder técnica): los tres ven las mismas cifras y las mismas filas en Personas, Células, Ausencias, Dedicación y Competencias. Verificar: captura de al menos dos perfiles distintos mostrando el mismo total.
- [x] 5.3 Repaso de textos en pantalla: ninguna card, estado vacío ni tooltip dice "chapter", y ninguna frase quedó incompleta al perder su complemento. Verificar: recorrido por las pantallas de los 16 textos corregidos.
