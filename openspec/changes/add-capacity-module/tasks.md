## 1. Prerequisito: tuip del front

- [x] 1.1 Subir `frontend/package.json` a `@tuya-ui/components` 0.2.3 y `@tuya-ui/tokens` 0.1.17 (tarballs existentes en `../tuip/.local-packages/`), `pnpm install --no-frozen-lockfile`, y ajustar lo que el salto 0.1.14→0.2.3 rompa (imports/props renombrados). Verificar: `pnpm install`, `pnpm typecheck` y `pnpm test` del front en verde todavía como app actual.

## 2. Front como remote (y adiós standalone)

- [x] 2.1 `vite.config.ts` del front: plugin `@module-federation/vite` con `name: "capacidad"`, `exposes: { "./module": "src/module/CapacityModule.tsx" }`, `shared` singleton estricto de `react`, `react-dom`, `react-router-dom`; script `dev` sirviendo el remote en 4300 y `build:prod` emitiendo `remoteEntry`. Verificar: `pnpm build:prod` emite el remote y `curl http://localhost:4300/remoteEntry.js` responde con `pnpm dev` arriba.
- [x] 2.2 `src/module/CapacityModule.tsx`: componente expuesto con props `{ source, acquireToken, basePath, topOffset }`; monta `HostAuthProvider source` + `CapacityPermissionsProvider` + rutas del módulo. `src/main.tsx` queda como arranque mínimo de desarrollo del remote (página que avisa que el módulo se abre desde el host) sin login ni simulador. Verificar: typecheck y un test de montaje del componente con puerto falso.
- [x] 2.3 `CapacityPermissionsProvider`: pide `acquireToken(["api://capacidad/access_as_user"])` al montar y ante cada cambio de `source`, decodifica el payload del JWT (base64url) y publica `permissions = mapCapacityRoles(claims.roles)` en el contrato; token ausente ⇒ `[]`. Verificar con tests: token con sub-claims → permisos exactos; token nulo → vacíos; cambio de sesión → re-derivación.
- [x] 2.4 Shell único `CapacityShell` con `ModuleShell` de tuip (`topOffset` de la prop): navegación unificada en `features/capacity-shell/navigation.ts` (Inicio; Iniciativas; Capacidad: Células, Personas, Ausencias, Dedicación, Facturación, Competencias; Configuración: Sprints, Parámetros, Habilidades, Líneas; DevOps: Ingesta) con el mapa sección→permiso existente, badge de Dedicación condicionado a su permiso, breadcrumb en el encabezado del contenido (provider generalizado desde el del lead). Verificar con tests: menú filtrado por permisos y grupos vacíos ocultos; entrada activa en ruta hija; breadcrumb compuesto en detalle de célula; sin barra propia (no hay banner del módulo).
- [x] 2.5 Rutas del módulo relativas bajo `basePath`: `<sección>` planas + hijas actuales, guards `RequirePermission` desde el mapa compartido, Inicio por permisos (torre de control con permisos de capacidad; estado de la plataforma con sólo configuración; aviso sin ninguno), y aviso de acceso no disponible si el contrato reporta sesión anónima. Verificar con tests: deep link a `celulas/:id` con permisos correcto; sin permiso → aviso; anónimo → aviso sin login.
- [x] 2.6 Borrar el standalone: `src/dev/auth-simulator`, `pages/Login*`, `AuthLayout`, `MainLayout`, `AdminLayout`, `ChapterLeadLayout`, `app/router` viejo (`createBrowserRouter`, `/auth/login`, `/app/*`), `AuthGuard` de redirección a login (queda el guard por rol/permiso), script `dev:auth`, `VITE_AUTH_SIMULATOR` de config y tipos. Reescribir los tests que dependían de eso contra el shell nuevo. Verificar: `grep -ri "simulator\|/auth/login\|AppShell" src` sin resultados en código de producción; `pnpm typecheck`, `pnpm lint`, `pnpm test` en verde.
- [x] 2.7 Mocks bajo el host: el registro del worker usa la condición de build del módulo y `serviceWorker.url` absoluto al origen actual; verificar además que el build de producción del remote no contiene chunks de `src/mocks`. Verificar: build de producción sin mocks; en el smoke (grupo 4) los datos cargan bajo el host.

## 3. Host como consumidor

- [x] 3.1 `host/`: dependencia `@module-federation/vite` (consumer, mismas versiones compartidas), `registry.ts` con `remoteEntry?: string` en `ModuleDefinition` y `capacidad` leyéndolo de `VITE_MF_CAPACIDAD_URL` (`.env.development`: `http://localhost:4300/remoteEntry.js`; `.env.example` documentada; vacío ⇒ placeholder). Verificar: `pnpm typecheck` y test del registry con y sin la variable.
- [x] 3.2 `RemoteModulePage` en el host: carga perezosa del remote declarado con `Suspense` (esqueleto bajo la barra), error boundary con "Reintentar" que re-crea el lazy, y paso del contrato `{ source: auth.source, acquireToken: auth.acquireToken, basePath, topOffset: alto de la barra }`; la ruta `/<basePath>/*` usa esta página cuando hay `remoteEntry` y el placeholder cuando no. Verificar con tests (remote mockeado): monta y recibe las props; fallo de carga → aviso con reintento y barra intacta; módulo sin remote → placeholder de siempre.
- [x] 3.3 Servir el worker de mocks del módulo en dev: copiar `frontend/public/mockServiceWorker.js` a `host/public/` (con nota de origen; sólo dev — excluirlo del build de producción del host o documentar que sin remote de dev no se registra). Verificar: con el trío corriendo, las llamadas del módulo responden datos de mocks.
- [x] 3.4 Redirecciones de cortesía en el host: `/app/admin/*` y `/app/lead/*` → `/capacidad`. Verificar con test de rutas.

## 4. Verificación integrada

- [x] 4.1 Suites completas: front (`pnpm test`, `pnpm lint`, `pnpm build:prod`) y host (ídem) en verde. Verificar: los seis comandos en 0.
- [x] 4.2 Smoke integrado (emulador + host + remote; capturas sobre builds+preview si el dev server congela Chrome): Ana entra a Gestión de Capacidad desde el portal → módulo real bajo la barra del host con sidebar completo y datos de mocks; Tomás ve el menú de sus 7 secciones y la torre de control de inicio; Lucía ve sus 4 secciones e Inicio como torre acotada, `/capacidad/facturacion` directo → aviso de permisos; deep link `/capacidad/celulas/<id>` con recarga completa resuelve; "Cerrar sesión" desde la barra del host sale del módulo; sin la variable del remote, `/capacidad` vuelve al placeholder; "Iniciativas y Células" sigue placeholder. Verificar: cada paso como se describe, un solo navbar en pantalla y sin warnings de federation en consola.
  > Resultado (Playwright sobre builds de desarrollo + `vite preview`, emulador desde `pnpm dev`): todo verificado salvo el paso de Lucía — su rol `tech-lead` no está en el registro del host para Capacidad (sólo `admin` y `chapter-lead`, y el diseño fija que el guard del host no cambia), así que el host la manda a "Sin permisos" antes de montar el módulo. El caso "sección sin permiso dentro del módulo" se verificó con Tomás en `/capacidad/sprints` (aviso en la ruta, sidebar intacto).
- [x] 4.3 READMEs: front (qué es el remote, contrato de montaje, ciclo emulador+host+remote, dónde viven los mocks) y host (variable del remote, cómo montar un módulo federado nuevo, el worker de mocks en dev). Verificar: ambos cubren esos puntos y ya no mencionan el standalone.
