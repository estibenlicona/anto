## 1. Backend: dominio y persistencia

- [x] 1.1 `backend/src/GestionCapacidad.Domain/Entities/Team.cs`: `AggregateRoot` con `Name` (obligatorio, ≤100) y `Description` (opcional, ≤500), constructor, `Rename(string)`, `UpdateDescription(string?)`, `EnsureRequired` igual que `Squad`. `Domain/Events/TeamEvents.cs`: `TeamCreatedEvent`, `TeamRenamedEvent`. Verificar: `dotnet build` sin errores; `tests/.../Domain/TeamTests.cs` cubre alta, longitud excedida, `Rename`/`UpdateDescription` y sus eventos.
- [x] 1.2 `Squad.cs`: reemplazar `Tribe: string` por `TeamId: Guid`, quitar `MoveTribe` y agregar `ChangeTeam(Guid teamId)` (mismo patrón que `ChangeCriticality`). `Domain/Interfaces/ISquadRepository.cs`: agregar `Task<bool> ExistsByTeamIdAsync(Guid teamId, CancellationToken ct = default)`. `Domain/Interfaces/ITeamRepository.cs` (nuevo): `ExistsByNameAsync`, `GetPagedAsync`. Verificar: `dotnet build`; `SquadTests.cs` ajustado (sin casos de `Tribe`, con `ChangeTeam`).
- [x] 1.3 `Infrastructure/Persistence/EntityConfigurations/TeamConfiguration.cs` (clon de `SquadConfiguration.cs`: `ToTable("Teams")`+`ToCollection("Teams")`, índice único por `Name`) y `DbSet<Team> Teams` en `ApplicationDbContext`. `SquadConfiguration.cs`: cambiar la property `Tribe` por `TeamId` con su FK a `Teams`. `Infrastructure/Repositories/TeamRepository.cs` (clon de `SquadRepository.cs`, sin filtro de criticidad) y `SquadRepository.cs`: implementar `ExistsByTeamIdAsync`, agregar filtro `teamIds` a `GetPagedAsync` y cambiar la búsqueda de texto para incluir el nombre del equipo referenciado. Verificar: `dotnet build`; `TeamRepositoryTests.cs` (SQLite in-memory, clon de `SquadRepositoryTests.cs`) y `SquadRepositoryTests.cs` ajustado.
- [x] 1.4 Migración de EF Core: `dotnet tool restore` (si hace falta) y `dotnet ef migrations add AddTeamsAndSquadTeamId --project src/GestionCapacidad.Infrastructure --startup-project src/GestionCapacidad.WebApi --output-dir Migrations` desde `backend/`. Editar el `Up()` generado para: crear `Teams`, sembrar los 4 equipos existentes, hacer el backfill de `Squads.TeamId` por nombre de `Tribe` antes de agregar el `NOT NULL`, y soltar la columna `Tribe`. Verificar: `dotnet ef database update` contra `gestioncapacidad_dev` sin errores (o `dotnet ef database drop` + `update` si el backfill local no calza, ver design.md); `dotnet test` con `PendingModelChangesTests` en verde (sin drift de modelo).
- [x] 1.5 `Infrastructure/Persistence/DevelopmentDataSeeder.cs`: nuevo arreglo `Teams` sembrado antes que `Squads`, resuelto por nombre; `Squads` usa el `Guid` del equipo sembrado en vez de la tupla `Tribe`. Verificar: arranque en Development siembra 4 equipos y 5 células con `TeamId` válido, sin duplicar en reinicios sucesivos.

## 2. Backend: casos de uso y contrato

- [x] 2.1 `Application/UseCases/Teams/{CreateTeam,UpdateTeam,GetTeams,GetTeamById}`: Request/Response/UseCase/Validator calcados de los de Squad (sin dependencias de agregados FTE). `DataTransferObjects/TeamDto.cs` (`Id, Name, Description, CreatedAtUtc, UpdatedAtUtc, SquadCount`), `Mappings/TeamMappings.cs`. Verificar con tests (Moq, sin DB): alta válida, nombre duplicado (400), edición válida, `NotFoundException` en `GetById`/`Update` inexistentes, listado paginado con búsqueda.
- [x] 2.2 `Application/UseCases/Teams/DeleteTeam`: `DeleteTeamUseCase` inyecta `ITeamRepository` + `ISquadRepository`; si `squadRepository.ExistsByTeamIdAsync(id)` es `true`, `throw new ConflictException(...)` con el nombre del equipo; si no, borra. Verificar con tests: borra sin células, `ConflictException` con células asociadas, `NotFoundException` si no existe.
- [x] 2.3 Ajustar los casos de uso de Squad que tocaban `Tribe` (`CreateSquadUseCase`, `UpdateSquadUseCase`, mapeos, validador) para usar `TeamId`/`ChangeTeam`; `SquadMappings.ToDto` agrega `TeamName` resolviendo contra `Team` (o recibe el nombre ya resuelto del caso de uso, según cómo quede más simple con el resto de `SquadAggregates`). Verificar con tests: `CreateSquadUseCaseTests`/`UpdateSquadUseCaseTests` ajustados y en verde; `teamId` inexistente responde 400.
- [x] 2.4 `WebApi/Endpoints/TeamsEndpoints.cs` (implementa `IEndpointDefinition`, se auto-registra): `GET /`, `GET /{id}`, `POST /`, `PUT /{id}`, `DELETE /{id}` con `.Produces(StatusCodes.Status409Conflict)`, sin `RequireAuthorization` (igual que el resto del backend). DI: los 5 casos de uso en `Application/DependencyInjection.cs`, `ITeamRepository`/`TeamRepository` en `Infrastructure/DependencyInjection.cs`. Verificar: `dotnet build`; smoke manual con `dotnet run` + `curl`/Swagger contra `/api/v1/teams`.
- [x] 2.5 `backend/oas.json`: paths `/teams` (los 5, con 409 en el DELETE) y schemas `TeamDto`/`CreateTeamRequest`/`UpdateTeamRequest`; ajustar `SquadDto` (`team` → `teamId` + `teamName`) y `CreateSquadRequest`/`UpdateSquadRequest` (`team` → `teamId`). Verificar: `npx @redocly/cli lint backend/oas.json` sin errores; `node backend/tools/contract-coverage.mjs` corre (puede marcar `/teams` sin consumidor, ver design.md).
- [x] 2.6 `backend/ENDPOINTS.md`: nueva sección "## Equipos" con la tabla de los 5 endpoints y link a `openspec/specs/teams/spec.md`; ampliar la nota 🟡 de Células con el cambio de `team` a `teamId`. Verificar: la fila del resumen general (`## Resumen`) suma el módulo Equipos.

## 3. Backend: verificación

- [x] 3.1 Suites completas del backend: `dotnet build` y `dotnet test` desde `backend/` en verde (las 9 fallas pre-existentes de `RestClientBehaviorTests` quedan igual, no relacionadas). Verificar: 0 errores nuevos, `PendingModelChangesTests` sin drift.

## 4. Frontend: mock y migración de Células

- [x] 4.1 `frontend/src/mocks/handlers/teams.seeds.ts`: los 4 equipos existentes (Ecosistema Digital, Riesgo y Fraude, Pagos, Datos y Analítica) con ids fijos, sin descripción. Verificar: exporta los 4 ids con los mismos nombres que usan hoy las semillas de `squads.seeds`/`squads.handlers`.
- [x] 4.2 `frontend/src/mocks/handlers/teams.handlers.ts`: `GET /teams` (paginado, búsqueda por nombre) con `squadCount` calculado contra el snapshot de células; `POST`/`PUT /teams` con validación de nombre (obligatorio, ≤100, único sin distinguir mayúsculas) y descripción (≤500); `DELETE /teams/{id}` con guard 409 si `squadCount > 0`, mensaje con la cantidad de células. Verificar con tests: alta, edición, nombre duplicado rechazado, eliminar sin células (200) y con células (409 con el conteo).
- [x] 4.3 `frontend/src/mocks/handlers/squads.handlers.ts`: reemplazar `team: string` por `teamId: string` en las semillas (usando los ids de 4.1) y calcular `teamName` en `enrich()` resolviendo contra el snapshot de equipos, con el mismo patrón que `activeInitiativesOf`. `squadService.ts`: `SquadDto.team` → `teamId`/`teamName`; `CreateSquadRequest`/`UpdateSquadRequest.team` → `teamId`. Verificar con tests: el listado de células devuelve `teamId` y `teamName` correctos; crear/editar una célula con un `teamId` inexistente responde 400.
- [x] 4.4 Búsqueda y filtro en `squads.handlers.ts`: el buscador combinado por texto matchea también contra `teamName` (resuelto por `teamId`), y nuevo filtro `teamId` (multivaluado) combinable con `criticality`. Verificar con tests: buscar por el nombre de un equipo encuentra sus células; filtrar por uno o más `teamId` devuelve sólo esas células, combinable con criticidad.

## 5. Frontend: contratos y adapters

- [x] 5.1 `frontend/src/features/teams/services/teamService.ts`: `TeamDto { id, name, description, squadCount, createdAtUtc, updatedAtUtc }`, `CreateTeamRequest`/`UpdateTeamRequest { name, description? }`, `list/create/update/remove`. Verificar: typecheck limpio, servicio consumido por un test de humo contra el mock de 4.2.
- [x] 5.2 `frontend/src/features/teams/adapters/TeamAdapter.ts`: entidad `Team` (mismo patrón que `Squad`), `TeamFormValues`, `toEntity/toFormValues/toCreateRequest/toUpdateRequest`, validación de nombre/descripción (`teamFormValidation.ts`, mismos límites que 4.2). Verificar con tests: mapeo completo, validación de campos requeridos y de longitud.
- [x] 5.3 `frontend/src/features/squads/adapters/SquadAdapter.ts` y `squadFormValidation.ts`: `team: string` → `teamId: string` en `Squad`/`SquadFormValues`; la validación de "equipo obligatorio" pasa de longitud de texto a "un `teamId` seleccionado". Verificar con tests existentes de `SquadAdapter.test.ts` ajustados y en verde.

## 6. Frontend: UI del módulo Equipos

- [x] 6.1 `TeamsStatsCards.tsx`: resumen con el total de equipos y el total de células agrupadas, sobre el conjunto completo (no la página/filtro). Verificar con tests: cifras correctas con y sin equipos.
- [x] 6.2 `TeamsList.tsx`: tabla paginada (nombre, descripción truncada, "N células"/"Sin células"), buscador por nombre, menú de fila (editar/eliminar), estados vacío inicial y "sin resultados", carga y error con reintento — mismo tratamiento que `SquadsList.tsx`. Verificar con tests: los escenarios de listado, búsqueda, vacío y error de la spec `teams`.
- [x] 6.3 `TeamFormDrawer.tsx` + `teamFormValidation.ts`: alta/edición en panel lateral con nombre y descripción (varias líneas), mismo patrón visual que `SquadFormDrawer.tsx` (encabezado, sección única, contador de obligatorios, precarga en edición). Verificar con tests: alta válida, validación de requeridos/longitud, error del servidor conserva los datos.
- [x] 6.4 `DeleteTeamConfirmDialog.tsx` + guard antes de abrir: si el equipo tiene células, mostrar el aviso que explica cuántas y no abrir el diálogo de confirmación; si no tiene, diálogo de confirmación normal. Verificar con tests: los tres escenarios de eliminación de la spec (sin células, bloqueada, cancelada) y el error del servidor.
- [x] 6.5 `TeamsContainer.tsx` + `hooks/{useTeams,useTeamMutations}.ts`: arma resumen + listado + drawer + eliminar, con el mismo cableado que `SquadsContainer.tsx` (breadcrumb actions, toasts de éxito/error, refetch de stats tras mutación). Verificar: `pnpm test` del feature en verde.

## 7. Frontend: formulario y listado de Células con el catálogo de Equipos

- [x] 7.1 `useTeams()` reexportado desde `features/teams` para consumo cruzado; `SquadFormDrawer.tsx`: el `Input` de texto "Equipo" se reemplaza por un `Select` poblado por ese hook, con el estado vacío ("no hay equipos, creá uno primero") cuando el catálogo está vacío. Verificar con tests: opciones del selector, estado vacío, alta/edición envían `teamId`.
- [x] 7.2 `SquadsList.tsx`: columna "Equipo" desde `squad.teamName`; segundo `FilterButton` "Equipo" junto al de Criticidad, poblado por `useTeams()`, combinable con criticidad y búsqueda. Verificar con tests: escenarios "Filtrar por equipo" y "Elegir varios criterios sin reabrir el filtro" de la spec `squads` actualizada.

## 8. Frontend: navegación y permisos

- [x] 8.1 `frontend/src/features/auth-session/capacityPermissions.ts`: agregar `"Equipos"` a `CAPACITY_PERMISSIONS`. Verificar: typecheck y test de `mapCapacityRoles` con el sub-claim `Capacidad.Equipos`.
- [x] 8.2 `frontend/src/features/capacity-shell/navigation.ts`: entrada "Equipos" en el grupo Capacidad (antes de Células), con su permiso y título de breadcrumb. `frontend/src/module/routes.tsx`: ruta `equipos` detrás de `RequirePermission`. Verificar con tests: la entrada aparece filtrada por permiso, ruta accesible/bloqueada según el permiso.
- [x] 8.3 `host/scripts/entra-local-seed.mjs`: sumar `Capacidad.Equipos` a los perfiles de Administrador, Líder de Expertise y Líder Técnico (Equipos es gestión de capacidad, no Configuración). Verificar: `pnpm entra:seed` corre sin errores y el nuevo sub-claim queda asignado.

## 9. Verificación final

- [x] 9.1 Suites completas del front: `pnpm test`, `pnpm lint`, `pnpm typecheck` en verde. Verificar: los tres comandos en 0.
- [x] 9.2 Verificación visual con mocks bajo el host o `pnpm dev:mock`: crear un equipo, verlo en el selector de Célula, filtrar Células por ese equipo, e intentar eliminar un equipo con células (bloqueado) y otro sin ellas (eliminado). Verificar: captura de cada paso.
- [x] 9.3 Confirmar que el backend real (`dotnet run` desde `backend/src/GestionCapacidad.WebApi`) responde `/api/v1/teams` con los mismos casos que el mock (alta, listado, edición, eliminar bloqueado/permitido), aunque el front todavía no lo consuma. Verificar: smoke manual con `curl`/Swagger, sin errores 500.
